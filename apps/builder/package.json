{
  "name": "@dify-projects/builder",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Spec 009 Builder backend — Lát 1 SKELETON (spawn 1 Implement phase + post-turn verify)",
  "scripts": {
    "dev": "tsx server/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.test.json",
    "test": "node --import tsx --test \"test/**/*.test.ts\""
  },
  "dependencies": {
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
