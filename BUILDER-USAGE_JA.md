# Builder — 使い方

## 0. すぐ使ってみる

> 　

1. アプリを起動します。どちらでも構いません。
   - プロジェクトのフォルダーでターミナルを開き、`bash scripts/update-and-run.command`
   - Finder で `dify-projects/scripts/` を開き、`update-and-run.command` を**ダブルクリック**
     （Windows は `update-and-run.bat`）
2. ターミナルの窓が開いて数十秒動きます。処理はすべて手元のパソコンで動きます。**この窓は閉じないでください** — 閉じるとアプリも止まります。
3. ブラウザが **http://127.0.0.1:4123** を自動で開きます。開かないときは、このアドレスを自分で入力してください。
4. 画面中央の入力欄に、作りたいものを書きます。例：
   *「文章を受け取って、3行の要約を返すワークフロー」*
5. `送信` を押します。ここから先は区切りごとに止まり、あなたの判断を待ちます。
6. 各フェーズのボタンを押すと次へ進みます。名前はフェーズごとに変わります。

   | フェーズ | 押すボタン |
   |---|---|
   | ① 要件を読み終えた | `仕様へ進む` |
   | ② 仕様を書き終えた | `この仕様で実装` |
   | ③ ファイルができた | `テストへ進む` |
   | ④ 検査が終わった | Dify の接続情報を設定済みなら `Dify にインポート`。まだ送りたくなければ `インポートせず完了` |

7. これで完了です。レポートと成果物は右のパネル、`成果物` から見られます。

---

## 1. 画面の全体像

> 【画像】画面全体：左のリスト、中央の会話、右の `成果物` パネル

画面は3つに分かれています。**左のリスト**、**中央の会話**（入力はここ）、そして**右の `成果物` パネル**
（見るものができた時点で開きます）。

どのビルドも **4つのフェーズ**を通り、それぞれの終わりで止まってあなたを待ちます。

| | フェーズ | すること |
|---|---|---|
| ① | `分析` | 要件を読み、似た事例を探す |
| ② | `仕様` | 何をするものなのかを文章にする |
| ③ | `実装` | 実際のファイルを作る |
| ④ | `テスト` | そのファイルが通るか検査する |

この停止点を**ゲート**と呼びます。ゲートでは、あなたが押すまで何も先へ進みません。

---

## 2. 左のリスト

> 【画像】5つの区画を開いた状態のサイドバー。`ビルド` の `+` にカーソル

| 区画 | 入っているもの | `+` ボタン |
|---|---|---|
| `進行中` | 実行中、またはあなたの操作を待っているビルド | — |
| `チャット` | 何も作らない、ふつうの相談 | `新規チャット` — 新しい相談を開く |
| `パターン` | 次回以降に使い回すひな形の置き場 | `外部YAMLを追加` — 外部のファイルを取り込む |
| `ビルド` | これまで作ったワークフローと、その履歴 | `新規タスク` — 新しく作りはじめる |
| `プロジェクト` | 整理用に自分で作るフォルダー | `新規プロジェクト` — フォルダーを作る |

`ビルド` の行にカーソルを合わせると、鉛筆の `新しい会話で編集` と削除ボタンが出ます。

---

## 3. 新しく作る

> 【画像】入力欄と、その下に並ぶ5つのチップ、右の `送信` ボタン

入力欄の下に小さな枠が5つあります。触らなくても動きます。`ワークフロー` `作成先` `高速ビルド` の3つは送信前にしか選べず、`モデル` と `確認` は途中でも変えられます。

| チップ | 何を決めるか |
|---|---|
| `モデル` | どのモデルに任せるか。既定のままで構いません |
| `ワークフロー` | 既存を直すのか、新しく作るのか。`なし（新規）` なら新規 |
| `作成先` | 作ったものを置くフォルダー。`Drafts` は下書き置き場で、まだ整理していない状態 |
| `確認` | `各ステップ` は4フェーズすべてで止まる · `仕様のみ` は②だけ · `自動` は止まらず最後まで |
| `高速ビルド` | `オン` にすると①と②をまとめて1回に。速くて安く、小さな用件向き |

クリップのボタンでファイルを添付できます。入力欄の下の `ベースにする` は、あなたの Dify にすでにあるアプリの一覧です。
ゲートで止まったときは、次へ進むボタンのほかに3つあります。

| ボタン | 押すとき |
|---|---|
| `修正を依頼` | **直してほしい**とき。この回でファイルが変わります。横の `⌄` で直し方を選べます — `すぐ直す`（直してから結果を見せる）か `先に計画を見せて`（何を変えるか先に説明し、あなたが決めてから直す。**実行 +1 回**） |
| `質問を送信` | **聞くだけ**のとき。答えは返りますが、**何も変わりません**。こちらが既定です |
| `仕様を修正` | （②のみ）右のパネルで仕様を自分の手で直したいとき |

---

## 4. 既存のものを直す

> 【画像】`ワークフロー` チップを開いた状態。既存ワークフローの一覧

やり方は3つ、結果は同じです。

| やり方 | 場所 |
|---|---|
| `ワークフロー` チップで選ぶ | 最初の画面 |
| 鉛筆の `新しい会話で編集` を押す | `ビルド` の行にカーソルを合わせる |
| `編集（新規）` を押す | 終わった会話の末尾 |

選んだら、直したい内容を書いて送るだけです。仕様とファイルがすでにあるものなら、画面に
`③ 実装から開始` と出ます。①と②を飛ばして実装から始まるという意味で、その分だけ短く済みます。

そのワークフローに途中のままの会話が残っている場合は、`開く` ボタン付きの一行が出ます。
新しい会話を始めずに、そちらへ戻れます。

---

## 5. 使い回せる棚に入れる

> 【画像】`外部ワークフローYAMLを追加` のモーダル。`用途` の2つの選択肢

YAML とは、ワークフローを書き表したファイルです。Dify が読む設計図のようなものだと思ってください。

`パターン` の棚には、次のビルドが参考にするひな形が入っています。入口は2つあります。

| 入口 | 場所 | すること |
|---|---|---|
| `パターンに昇格` | 終わった会話の上部のボタン | **1つの**ワークフロー — その会話のもの — をひな形にする |
| `外部YAMLを追加` | `パターン` 区画の `+` | 外部の YAML ファイルを取り込む |

`外部YAMLを追加` では、先に `用途` を選びます。

| 選択 | 結果 |
|---|---|
| `パターン棚に蒸留` | 棚に入れるひな形として抜き出す |
| `取り込んで編集` | あなたのフォルダー（既定は `Drafts`）に置き、そのまま編集を始める |

**間違えやすい点：** `パターン棚に蒸留` は、あなたのフォルダーにファイルを置き**ません**。ひな形を作るだけで、
しかもゲートで承認するまで保存されません。そのファイル自体を直したいなら `取り込んで編集` を選んでください。

---

## 6. そのほか

> 【画像】`成果物` パネルの `main.yml` タブ。3つのタブと `エクスポート` ボタン

| もの | 場所 | 何のため |
|---|---|---|
| `相談` | `チャット` 区画 | 何も作らないふつうの相談。ビルドの実行回数は減りません |
| `仕様` タブ | `成果物` パネル | 何をするものかの説明。手で直せます |
| `main.yml` タブ | `成果物` パネル | 実物のファイル。前回との差分も見られます |
| `レポート` タブ | `成果物` パネル | ④の検査結果 |
| `エクスポート` | ヘッダー | ビルドの記録をダウンロード／共有。各フェーズの経過と時系列 |
| `停止` / `ビルドを破棄` | ヘッダー | ビルドを終える。**同じ1つのボタン**で、状況によって顔が変わります（下記） |
| `Dify にインポート` | ④のゲート | あなたの Dify へ送る |
| `フェーズ完了通知` | 上部のベル | フェーズが終わったら知らせます — **ほかのタブを見ているときだけ**。オンなのに出ないときはブラウザ側でブロックされています。サイト設定で許可してください |
| 更新ボタン | サイドバー上部 | Builder の新しい版を取得して再起動 |

**終了ボタンは二面あり、結果が違います**（確認ダイアログに出ます）。**実行中**に押すと `停止` で、**そのフェーズの進捗は破棄されます**。
**ゲートで待っている**ときに押すと `ビルドを破棄` で、仕様もファイルも**残ります**。あとから会話画面で開き直せます。

**間違えやすい点：** `Dify にインポート` は、接続情報を設定した Dify に**新しいアプリを作ります**。**同じビルドから**
もう一度押した場合は、そのとき作ったアプリが更新されるだけで、増えることはありません。ただし、あなたが以前から
持っている別のアプリを上書きすることは決してありません。

---

## 7. よく出る言葉

| 言葉 | 意味 |
|---|---|
| `ワークフロー` | 出来上がった処理の流れ。Dify で動かすもの |
| YAML | ワークフローを書き表したファイル。Dify が読む設計図のようなもの |
| `ベース` | ゼロからではなく、出発点にする既存のもの |
| `パターン` | 棚に入れたひな形。次のビルドが参考にする |
| `蒸留` | 実物のファイルから、使い回せるひな形を抜き出すこと |
| `プロジェクト` | ワークフローを整理するために自分で作るフォルダー |
| `Drafts` | 下書き置き場。行き先を決めていないものが入る |
| ゲート | 各フェーズの終わりで、あなたの操作を待って止まる場所 |
