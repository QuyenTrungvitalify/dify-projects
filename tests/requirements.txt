# Pytest harness deps — single source of truth is the repo-root requirements.txt
# (locked from requirements.in, spec 024 Q1b). Kept as a pointer so existing
# `pip install -r tests/requirements.txt` still works.
-r ../requirements.txt
