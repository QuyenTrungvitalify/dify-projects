# #10 · dify_course_demo.yml · run 1782559476926 · status done

**Prompt:** 「チュートリアルのテーマ名を入れたら、章立てから本文まで丸ごと自動生成するワークフローを作りたい」
*(Enter a tutorial topic; auto-generate the whole thing from outline to body.)*
**Slug:** `workflow_6` · **Mode:** workflow · **Ground truth:** `dify_course_demo.yml`

## Run status — FIRST
Run is **`done`** (auto-confirm), NOT parked. It reached phase ④ and completed cleanly: all 3 linters 0/0/0 and `idsOk` passed. This is the **first iteration-shaped run to auto-complete after the #9 gate fix** (`/^\d{13}(start)?$/`): the iteration-start id `1782559789920start` no longer false-parks the run at `still_failing`. No user action required.

## Per-phase (process + output)

**① Analyze ✅** — 5 tool calls. Wrote **only** `analyze.json` (`touched_workflow_file: false`). Honest from-scratch: `seed:null`, `pattern: custom`, features `[llm, iteration, code]` — sensibly anticipates the outline→iterate→assemble shape. **2 errors, both benign:** 1 `shell-metachar: command chaining` (an `ls … ; echo … ; ls …` rejected by the harness), 1 `file-not-found probe` (`ls …/.runs/<id>/` — directory not yet created). No fabricated `find_query`/`change_points`.

**② Spec ✅** — 10 tool calls. **Searched the pattern library** (`find.py --has llm --has iteration`, read `templates/patterns/file-iteration.yml`, listed `templates/patterns/` + `templates/library/`, read corpus `dify_course_demo.yml` as a *purpose reference*). Did **not** mint IDs or write YAML (`minted_ids: false`, `wrote_yaml: false`). **1 error, benign:** `shell-metachar: command chaining` (`ls … ; echo … ; ls …`). Output: node table covers all 3 `must_do`; lean per-**chapter** design (caps outline ≤8 to stay under Dify's ≤30 iteration limit), 5 open questions surfaced (granularity / model / output format / language / provider). Honest plugin handling (`dependencies: []` + `# TODO hash`, never fabricated).

**③ Implement ✅** — 28 tool calls. **Minted 9 IDs via `generate_id.py 9`** (exactly the distinct node count; iteration-start reuses the iteration id + `start`). Ran **all three validators**, each clean. **9 errors, ALL benign:** 8 `shell-metachar` harness rejections (chaining/redirect/pipe/unclassified — e.g. `find … 2>/dev/null`, `sed -n '1,400p' AGENTS.md`, `grep -n "…|…"`, `lint_refs … ; echo "refs=$?"`, `lint_refs && lint_plugin_hashes && echo ALL_CLEAN`) + 1 `file-not-found probe` (`Read SKILL.md` at repo root — correctly probed, then used Glob/find). **`lint_refs` ran 3× = the chained exit-code variants rejected, then re-run plainly — NOT a real mid-run fix** (each plain validator run was clean on the first try). YAML realizes the spec node table **exactly** — no silent add/drop.

**④ Test ✅** — backend-run (no transcript). Re-ran validators from repo root → `validate 0`, `lint_refs 0`, `lint_plugin_hashes 0`. Matches `report.json.lint`.

## Requirement-fit (the headline) — 3/3 met

| # | must_do | verdict | evidence |
|---|---|---|---|
| 1 | accept a topic name | ✅ met | Start `course_name` (`text-input`, required, max 48) — [main.yml:99-108](../../../../projects/workflow_6/workflows/main.yml#L99) |
| 2 | generate an outline / chapter structure | ✅ met | LLM "Chapter Outline" emits JSON array of `{chapter,title,contents[]}`; "Split Chapters" code parses → `array[object]` — [main.yml:117](../../../../projects/workflow_6/workflows/main.yml#L117), [:159](../../../../projects/workflow_6/workflows/main.yml#L159) |
| 3 | generate body content for each section (iterate) | ✅ met | Parallel iteration over chapters → LLM "Chapter Body" writes full prose per chapter → merge → assemble — [main.yml:222](../../../../projects/workflow_6/workflows/main.yml#L222), [:304](../../../../projects/workflow_6/workflows/main.yml#L304) |

**Avoided the partial-miss trap** (`allow_diff`: "a single LLM dumping everything in one shot is a PARTIAL miss"): this build does **real section-by-section generation** via a genuine iteration loop, not a one-shot dump. No genuine misses.

## Validity & lint
- Re-run: `validate 0` · `lint_refs 0` · `lint_plugin_hashes 0` — **agrees with `report.json`**. All `{{#id.field#}}` refs resolve to declared upstream `outputs` (incl. container-scope refs inside the iteration). Node ids = 13-digit quoted timestamps; iteration-start = `1782559789920start` (valid per §4.1 / post-#9 regex).
- **Runnability blockers:** 1 — `unresolved plugin TODO` (`dependencies: []` + `# TODO hash`). **Models are pre-filled** (`openai/gpt-4o` outline, `openai/gpt-4o-mini` bodies — `model_empty: false` for both), so the usual empty-model blocker does **not** apply. No sandbox-illegal code (only `json`, stdlib). No KB/external tools.

## Structure vs ground truth (secondary)
Near-identical histogram — `code 4` vs GT `3` (+1: the build splits unpack/merge/assemble; GT has an untyped node), all else equal: `llm 2=2`, `iteration 1=1`, `iteration-start 1=1`, `start 1=1`, `end 1=1`. Node count **10 = 10**, mode `workflow = workflow`. Unusually high fidelity for a lean build — it lands almost exactly on the (well-shaped) reference.

## What this workflow actually does (from the graph)
1. **Start** takes a tutorial theme `course_name`.
2. **LLM (gpt-4o)** emits a JSON chapter outline (≤8 chapters, each `{chapter, title, contents[]}`).
3. **Code "Split Chapters"** strips ```json fences, `json.loads`, normalizes → `array[object]`, hard-caps ≤30.
4. **Parallel iteration** (3-wide, continue-on-error, flatten): per chapter → unpack fields → **LLM (gpt-4o-mini)** writes the full chapter body → code wraps it as `## N Title\n\nbody`.
5. **Code "Assemble Tutorial"** joins all blocks under a `# course_name` H1.
6. **End** outputs `tutorial` (full markdown), `outline` (raw JSON), `chapter_count`.

## Runtime — `NOT VERIFIED` (manual spot-check)
Build is `deploy=none`; /report does not auto-run. **Manual run kit:**
- **Pre-run:** ensure the **`langgenius/openai`** plugin is installed + API key set in the target workspace; optionally add its `<ver>@<sha256>` to top-level `dependencies` (or just import — Dify resolves provider `openai` if the plugin is installed). Models already set (swap to Anthropic/Gemini if preferred — Open Q5).
- **Import:** Studio → Import DSL → `projects/workflow_6/workflows/main.yml`.
- **`sample_input`:** `course_name` = `Python のデコレータ入門`.
- **`expected_signals`:** a structured tutorial (outline + per-section body), coherent progression, covers the topic. Grade `END.tutorial`.
- **`runtime_note`:** iterates over chapters (≤8, capped ≤30) — can be slow; `parallel_nums: 3`.

## Needs-improvement
Only the **advisory plugin TODO**: add `langgenius/openai:<ver>@<sha256>` to `dependencies` (or install the plugin in the workspace) before deploy — `report.json` already flags `unresolved_plugin_todo`. No genuine misses, no lint/schema/ref/runnability defects, no procedure violations.

## Verdict
**PASS (high).** All 3 `must_do` met with real outline→iterate→body generation (avoided the single-LLM partial-miss trap), lint 0/0/0, refs sound, structure near-identical to the ground truth, **models pre-filled**. First iteration run to auto-complete post-#9 gate fix. Result-quality (does the generated tutorial read well?) is **unverified** until a manual run is graded via `/report #10 output: …`.
