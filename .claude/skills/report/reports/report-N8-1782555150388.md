# Report — #8 · matplotlib.yml · run 1782555150388 · status done

**Requirement (prompt_jp):** 数値データを渡したら、Pythonでグラフを描いて画像で返すチャットを作って
**Gloss:** Pass numeric data, draw a chart with Python, return it as an image.
**Slug:** `python` · **Mode:** advanced-chat · **deploy:** none
**type_focus:** code execution / chart (advanced-chat) — **THE §4.5 sandbox-trap test**

> Headline: this is the one test designed to catch *Tier-1-looks-fine-but-Tier-2-breaks*.
> The builder did the right thing — it **honestly avoided matplotlib** and hand-built an
> SVG chart in pure stdlib (`base64` only), so the code node is sandbox-legal. No fabricated
> unrunnable `import matplotlib`. Best possible outcome for #8.

---

## Per-phase (process + output)

**① Analyze — ✅** 4 tool calls (Glob/Read SKILL.md, 1 Bash, Write analyze.json).
- **Process:** procedure-clean — `touched_workflow_file: false`, `only_wrote_analyze_json: true`. No YAML.
- **Errors:** 1, benign — `ls -d .runs/… && echo …` → `class: shell-metachar: command chaining`. Harness rejection, not a defect.
- **Output honest:** from-scratch ⇒ `seed:null`, empty `nodes/var_flow/plugins`, no invented `find_query`/`change_points`. `features:["llm","code"]`.
- **Standout:** the forward-looking risks **nailed the §4.5 trap before any code existed** — *"Code nodes run a stdlib-only sandbox … matplotlib/numpy/pandas are NOT importable … the Spec phase must decide how an image is actually produced (charting tool, external HTTP service, or stdlib-only SVG/base64) rather than assuming matplotlib."* Also flagged image-as-variable typing and the §4.1 ID-minting rule. Exactly the right anticipation.

**② Spec — ✅** 10 tool calls.
- **Process:** `searched_patterns: true` (ran `find.py` 5×: `--list-features`, code+answer, code+llm+answer, http-request+chart, parameter-extractor+code), read AGENTS.md. `minted_ids: false`, `wrote_yaml: false`. Clean.
- **Errors:** 1, benign — `find.py --list-features 2>&1 | head -60` → `class: shell-metachar: pipe (|)`; re-ran plainly immediately after.
- **Output:** turned the trap into the *design driver* — "draw a graph in Python" → **Python builds an SVG string by hand → base64 data-URI markdown image**. Node table covers every must_do. Documented fallbacks (PNG data-URI; QuickChart HTTP — explicitly noted as moving "drawing" off Python). 4 open questions surfaced, incl. the make-or-break: **does Dify's chat renderer display `data:image/svg+xml;base64` inline?** Honest about the parameter-extractor-vs-pure-`re` trade (kept the extractor for NL tolerance; flagged the zero-plugin alternative).

**③ Implement — ✅** 44 tool calls (deep schema due-diligence: read vendored `parameter_extractor/entities.py`, `node_types.md`, `constraints.md`, `FormChat.yml` for the branch shape).
- **Process:** `ran_generate_id: true`; all three validators ran (`validate_runs:1`, `lint_refs_runs:3`). Realizes the SPEC node table **exactly** (7 nodes, no drift).
- **Errors:** 6, **all benign harness rejections — zero real problems:**
  1. `ls -R … | head -50` → `shell-metachar: pipe (|)`
  2. `find … -type f` → `dangerous-executable rejection` (find)
  3. `grep -n "parameter-extractor" -A 60 … | head -80` → `shell-metachar: pipe (|)`
  4. `readlink …/vendor/dify-src` → `allow-set (script run by absolute path / not whitelisted)`
  5. `grep -rl "class ParameterExtractorNodeData" …` → `shell-metachar (unclassified)`
  6. `lint_refs.py …; echo "lint_refs exit=$?"; lint_plugin_hashes…` → `shell-metachar: command chaining (&& / ; / ||)`
- **`lint_refs_runs:3` explained (no mid-run fix):** ran lint_refs standalone (pass) → tried to chain lint_refs+lint_plugin_hashes (rejected, error #6) → re-ran lint_refs individually (pass). Innocent re-run after a chaining rejection, corroborated by `--full`. The phase's "all three pass" final claim matches the actual exit codes.

**④ Test — ✅** backend-run, no transcript. Re-ran validators (Step 4) → all green.

---

## Requirement-fit (the headline) — 3/3 met

| must_do | verdict | evidence |
|---|---|---|
| accept numeric data | ✅ met | `parameter-extractor` (id `…494`) pulls `numbers: array[number]` from `sys.query`, NL-tolerant (handles 「売上は12, 19, 7」 / 全角 区切り) |
| generate a chart **via a code node** | ✅ met | `code` python3 node (id `…498`) hand-builds bar/line SVG geometry — genuinely "Python draws the graph" |
| return it as an **image** | ✅ met | code returns `![title](data:image/svg+xml;base64,…)`; `answer` node renders `{{#…503.output#}}` inline |

**allow_diff:** *"Mechanism free (matplotlib vs echarts markdown vs SVG string)."* → SVG string is **explicitly permitted**. The added input-validation branch (no-numbers → ask) and NL `parameter-extractor` are enrichment beyond the bare must_do, not misses.

**§4.5 trap verdict — PASSED (the point of this test):** builder chose **(a) honestly avoid matplotlib** — `code_nodes[0].imports: ["base64"]`, `nonstdlib_imports: []`, `sandbox_trap: false`. It did **not** fabricate an unrunnable `import matplotlib` node. The code defensively coerces upstream values (None/str/mixed) and degrades gracefully on empty/single-point data.

---

## Validity & lint (re-run from repo root)

| check | my re-run | report.json | agree |
|---|---|---|---|
| validate_workflow.py | exit 0 ✅ | 0 | ✅ |
| lint_refs.py | exit 0 ✅ | 0 | ✅ |
| lint_plugin_hashes.py | exit 0 ✅ | 0 | ✅ |

- Node IDs = 13-digit ms-timestamp quoted strings (`'1782555489491'`…) ✅ (§4.1).
- All `{{#id.field#}}` refs resolve (lint_refs 0): aggregator pulls `…498.image_md` ∥ `…500.output`; answer reads `…503.output`. ✅
- if-else emits both legacy `conditions` + modern `cases` (documented §9 quirk) — validator accepts.
- **Runnability blockers (2):** `parameter-extractor.model.provider/name` empty (`# TODO`); `dependencies: []` + `# TODO` hash → `unresolved_plugin_todo: true` (matches report.json). Code node has **no** trap.

## Structure vs ground truth (secondary, context only)

Histogram delta — builder **7 nodes** vs GT `matplotlib.yml` **3 nodes** (start, code, answer):
`+parameter-extractor +if-else +template-transform +variable-aggregator`. Mode matches (advanced-chat ✅).
Builder is *richer* than the store file (NL extraction + graceful no-data branch), not a clone — fine; GT extras/omissions are reference-only. Note the store's own node literally `import matplotlib` (would hit the sandbox trap on a stock sandbox); the builder's stdlib-SVG path is the **more runnable** reading.

## What this workflow actually does (from the YAML graph)

`start` → `parameter-extractor` extracts `numbers/title/chart_type` from the chat message →
`if-else` (numbers not empty?): **true** → `code` hand-builds an SVG bar/line chart, base64-encodes it into a `data:image/svg+xml` markdown image; **false** → `template-transform` emits a friendly "send me numbers" prompt → `variable-aggregator` merges whichever branch ran → `answer` renders it inline in chat.

## Runtime — `RUNTIME: NOT VERIFIED` (manual spot-check)

**Not runnable as-is. Blockers:**
1. `parameter-extractor` `model.provider`/`name` are empty → fill with an installed chat model.
2. `dependencies: []` + `# TODO` → add that model provider's plugin `@sha256` from the target workspace.
3. Code node: **no blocker** (base64 is stdlib) — the trap was avoided. ✅

**Manual run kit:**
- **Pre-run edits:** set `…494.model.provider`/`name` (e.g. `langgenius/openai` + `gpt-4o-mini`, or `langgenius/anthropic` + a Claude chat model) to something installed; add its plugin hash to top-level `dependencies[]`.
- **Import:** `projects/python/workflows/main.yml` → Dify Studio → Import DSL (no `selfhost` creds on this run).
- **sample_input (paste in chat):** `売上: 1月120, 2月150, 3月90, 4月200。棒グラフにして。`
- **expected_signals:** a chart image (or renderable chart markup) returned; reflects the 4 data points.
- **runtime_note / #1 make-or-break:** **does Dify's chat markdown renderer display a `data:image/svg+xml;base64,…` image inline, or sanitize/block the data-URI?** If blocked, the image won't render (spec's fallbacks: PNG data-URI, or QuickChart HTTP). Verify by eye on the manual run — this is exactly what Tier-2 exists to catch for #8. Also confirm `base64` is in the sandbox stdlib whitelist (expected-present, listed as unverified in the implement notes).

To grade a real run: `/report #8 output: <pasted chat output / screenshot description>`.

---

## Needs-improvement

No real defects. Lint clean (re-verified 0/0/0), all 3 must_do met, IDs/refs correct, **no §4.5 trap**, provenance honest, procedure followed at every phase, all 8 traced errors benign.

Two non-defect observations:
- **Leaner alternative existed (by design):** the `parameter-extractor` is the *only* thing making this non-runnable out of the box. A pure-`re.findall` parse inside the code node (spec open-Q #3) would be zero-plugin + fully deterministic. The builder consciously kept the extractor for NL tolerance — a defensible call, just the one model dependency to fill.
- **#1 runtime risk is unverified by nature:** SVG data-URI inline rendering in Dify's chat UI is the make-or-break and can only be confirmed by a live run. Honestly flagged in both SPEC and implement notes.

## Verdict

**PASS (clean) — the §4.5 trap test handled correctly.** Built right: lint 0/0/0, exact spec→YAML fidelity, 3/3 must_do, sandbox-legal code node (SVG, not matplotlib), honest provenance. **Result-quality is unverified** until a manual run grades the actual chart image — and the one genuine open risk is whether Dify renders the SVG data-URI inline. Fill the model + plugin hash, run the sample, and `/report #8 output: …` to close Tier-2.
