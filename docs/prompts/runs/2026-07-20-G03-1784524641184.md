# G03 — Tổng hợp điểm test an toàn: webhook + nhắc <80 + tổng hợp cuối tháng

**Prompt**: [gen/2026-07-20-quiz-gen/G03](../gen/2026-07-20-quiz-gen/G03-quiz-result-aggregate.md) · **Run**: `1784524641184`
**Mode**: `auto` · deploy none · model: haiku cả 3 phase (so sánh nội-run hợp lệ)

| Phase | Model | Turn | Denied |
|---|---|---|---|
| ① | haiku-4-5 | 10 | 1 |
| ② | haiku-4-5 | 11 | 4 |
| ③ | haiku-4-5 | 30 | 8 |

**Lint 4/4 sạch — NHƯNG chỉ cho `main.yml`** (xem finding A) · comprehension PASS
Out: `google_1_80_chatwork/workflows/main.yml` **+ `monthly_summary.yml`** (file thứ hai!)

## Trục 1: đàm phán phạm vi 2-nhịp — ① XUẤT SẮC, ④ GÃY

① tự khai đủ **cả 5 bẫy đã gài** không sót cái nào: 1-trigger-1-flow → tách 2 workflow ·
stateless cần kho ngoài · field webhook 要確認 (đúng luật open-point 072 S3) · 未受験者 cần danh
sách ngoài Form · UTC→Asia/Tokyo. SPEC có Open questions Q1–Q6 minh bạch. **Đây là mẫu đàm phán
phạm vi tốt nhất từng ghi nhận.**

Nhưng judge (context sạch) phát hiện: **④ bỏ rơi im lặng workflow B** —

## 🔴 Finding A (nặng nhất đợt): tầng ④ giả định MỘT file/workflow

Build tạo 2 file, còn `report.json` chỉ khai `workflow_file: main.yml`:
- `monthly_summary.yml` **không qua 4 linter** (không có bằng chứng kiểm nào);
- notes **không nhắc nó tồn tại**, không nhắc `ADMIN_ROOM_ID` nó cần;
- user không kỹ thuật sẽ **triển khai đúng nửa hệ thống (A) và không biết nửa (B) bị bỏ sót**.

Cùng lớp "silent import success" leo thêm một tầng: build đúng + trung thực ở ①/②, mà **báo cáo
cuối nói dối bằng cách im lặng**. `campaign.py record` cũng chỉ gặt một `workflow_file` — giả định
1-file lan cả sang harness.

## Trục 2: advisory nguồn webhook (v0.2.0) — BẮN ĐÚNG

Notes chứa nguyên văn advisory với **đúng field list**: `emp_id, name (required), email,
score (required), submitted_at, room_id (required)` → mẫu sống #1 cho gate hồi quy (finding 9,
cần ≥2 đợt). Judge trừ: notes không giải thích `room_id` là gì/tra ở đâu — mắt xích khó nhất với
người không kỹ thuật.

## Judge verdict: PARTIAL — thêm 2 lỗi thật trong file B

1. **Aggregate không lọc theo tháng**: `target_month` tính ở guard nhưng code tính avg/min/未受験
   trên TOÀN BỘ sheet → **số liệu sai từ tháng thứ 2 trở đi**, TODO không cảnh báo.
2. Ghi điểm: `read_log` output **không được dùng**, `batch_update` range tĩnh `記録!A:D` — chính
   comment trong file thừa nhận có môi trường sẽ **ghi đè từ A1** (rủi ro mất dữ liệu).
Điểm cộng: schedule `Asia/Tokyo` tường minh ✅ · giọng mềm cài đúng chỗ (system prompt cấm 叱責) ✅
· so điểm bằng code, LLM chỉ viết văn ✅ · room group tách khỏi room cá nhân ✅.

## MANUAL còn nợ

Apps Script + token/room thật · nộp 79/80/81 thử ngưỡng · hành vi append vs ghi-đè của batch_update
· format thật của batch_get · giọng văn thực (model đang rỗng).

**Verdict: PARTIAL** — thiết kế + đàm phán hàng đầu; gãy ở ④ (finding A) và 2 bug file B.
