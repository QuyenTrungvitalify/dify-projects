# R4-G01 — Edit-existing: thêm log Sheets vào flow EC đã build, giữ nguyên routing (JA)

**Prompt**: [gen/2026-07-20-lifecycle/G01](../gen/2026-07-20-lifecycle/G01-edit-add-sheet-log.md) · **Run**: `1784557273931`
**Base (seed)**: chính output **round-3 G01** (`5_chatwork_5_52`) — chuỗi vòng đời thật build→sửa
**Model**: haiku cả 3 · ①9t ②15t ③21t · denied≈ 0/3/4 · **Lint 4/4 sạch** · 1 file

## Trục: edit-existing giữ nguyên hành vi cũ — ĐẠT XUẤT SẮC

Judge (context sạch) so seed↔built từng node:
- **7/7 node gốc byte-identical** (webhook→parse→if-else ≥5万→2 nhánh DM→aggregator→end) — id +
  config không đổi một byte. Ràng buộc 「今の動きは変えないで」 tôn trọng tuyệt đối.
- **2 node thêm** (code dựng 1 hàng + tool `batch_update`), chèn **sau aggregator** — điểm hợp lưu
  DUY nhất của 2 nhánh → mọi đơn log đúng **1 lần**, không sót nhánh nào, không nhân đôi. Nối đúng.
- 6 cột đúng thứ tự 日付・注文番号・商品名・金額・お客様名・送り先; nhãn 送り先 tính lại bằng
  code cùng ngưỡng 50000 → khớp đích thật.
- Nếu ghi Sheet lỗi thì DM đã gửi xong trước đó (thứ tự đúng) — hành vi cũ an toàn.

Đây là kết quả tốt nhất về trục edit-existing (round-2 P12 từng lỗi digest-EN; giờ edit sạch).

## Cần chạy thật

`batch_update` **append hay overwrite** (node không truyền sheet name/range/append flag — SPEC tự
khai chưa chốt). Nếu overwrite thì "月末見返す" hỏng dù cấu trúc đúng. Cùng họ nghi vấn plugin-behavior
(finding 7). Credential Google + SPREADSHEET_ID lúc deploy.

**Verdict: PASS** (1 điểm runtime cần verify) · slug edit giữ tên base (không sinh slug rác — vì có base).
