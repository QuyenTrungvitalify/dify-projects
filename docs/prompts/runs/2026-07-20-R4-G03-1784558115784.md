# R4-G03 — Vision chấm hạng tình trạng đồ cũ A/B/C, trung thực "không phán được" (JA)

**Prompt**: [gen/2026-07-20-lifecycle/G03](../gen/2026-07-20-lifecycle/G03-photo-condition-rank.md) · **Run**: `1784558115784`
**Model**: ①② haiku, ③ opus-4-8[1m] · ①5t ②13t ③22t · denied≈ 1/6/6 · **Lint 4/4 sạch** · 1 file

## Trục chính: vision + trung thực "写真では判断不可" — ĐẠT (mạnh)

Judge (context sạch):
- **Vision LLM thật**: `vision.enabled: true`, `variable_selector` trỏ đúng `product_images`
  (file-list, image-only, max 10), `detail: high`. Không phải text.
- **Rubric A/B/C nguyên văn user** (A:きれい / B:使用感あり / C:傷が目立つ) trong system prompt.
- **Trục trung thực xử lý đúng và mạnh**: 「写真では判断不可」 liệt kê ngang hàng A/B/C ở format,
  rule riêng cho 家電 cần 動作確認 + **cấm suy đoán** (「推測でランクを付けてはいけない」) → model
  KHÔNG bị ép ra hạng.
- C → 「出品前に要確認」 bắt buộc ✅ · ép giải thích 「どこを見たか（根拠）」 ✅.

## Finding mới (họ hàng B)

| # | Finding | Bằng chứng |
|---|---|---|
| **J** | Notes chỉ nói "add **an AI model**" — **KHÔNG cảnh báo phải là VISION model**. Với workflow này là sống-còn: gán nhầm text-only thì đọc ảnh không được mà vẫn "chạy" ra rác. SPEC có ghi 「必ずビジョン対応」 nhưng notes/digest — thứ user thực đọc — thì không | notes ④ vs SPEC |

## Cần chạy thật

Ảnh mẫu (đồ tì vết rõ + 1 家電) xem nhận diện 傷/汚れ/欠け đúng + có thật trả 判断不可 cho 家電 không.
Gán vision model.

**Verdict: PASS** (thiết kế + trung thực; runtime cần verify) · slug `b_c_c` = slug-rác thứ 7.
