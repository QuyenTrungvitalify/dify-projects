# Campaign v0.2.0 — `2026-07-21-tiebreak` (đợt 6) · phá hòa 2 finding, KHÔNG mở trục mới

Đối chiếu: [CAMPAIGNS.md](CAMPAIGNS.md) · đề: [gen/2026-07-21-tiebreak/](../gen/2026-07-21-tiebreak/)
· gói fix: [spec 074](../../specs/074-campaign-findings-consolidated-fix.md)

Mục đích duy nhất: chốt **finding I** (mất dấu VI — đang 1 lỗi/1 sạch) và **finding A** (④ giả định
1-file — n=1). 3/3 done, lint sạch, 0 fail hạ tầng.

| Đề | Judge | Kết luận cho tiebreak |
|---|---|---|
| G01 kho VI (`kho_b_n_m`, task 1784640959120) | **PARTIAL** | **I tái hiện — mất dấu 100%** |
| G02 CV VI (`b_n_m_nh_4`, task 1784641409761) | **PASS** | I **không** tái hiện — dấu đúng 100% |
| G03 2-nhịp JA (`chatwork`, task 1784641794909) | **PASS** | **A KHÔNG tái hiện** — 1 file |

## Chốt finding I: CÓ THẬT, ~50%, locus rõ

4 mẫu VI qua 3 đợt: R4 mất · R5 giữ · **R6-G01 mất (0 ký tự có dấu / 34 cụm không dấu)** · R6-G02
giữ (349 ký tự có dấu). Cùng version, cùng ngày, cùng ngôn ngữ ⇒ **ngẫu nhiên, không phải giới hạn
hệ thống**.

Judge G01 khu trú chính xác: **`SPEC.md` tiếng Việt có dấu hoàn hảo, `main.yml` thì 0 dấu** ⇒ lỗi
nằm ở **khâu sinh YAML**, không phải năng lực ngôn ngữ. Và **không phải lỗi thẩm mỹ**:
- chuỗi mất dấu nằm ngay trong **system prompt điều khiển văn phong email gửi nhà cung cấp** → model
  có thể bắt chước, xuất email B2B không dấu;
- label/hint/title trên UI thì **chắc chắn** sai, user nhìn mỗi sáng.
⇒ Nâng từ "không tất định, đừng fix" thành **fix bằng guidance** (spec 074 S4).

## Chốt finding A: KHÔNG tái hiện ⇒ hoãn S3

Đề 2-nhịp G03 (thiết kế để dụ multi-file như R2-G03) ra **1 file**. Theo luật dừng của spec 074:
A giữ n=1 ⇒ **không sửa hạ tầng report/wire cho một ca đơn lẻ**.

**Nhưng lộ biến thể mới (A′)**: file 1-file-2-trigger, mà notes viết *"the trigger"* **số ít** →
user có thể chỉ bật 1 trong 2 trigger và **âm thầm mất một nhịp**. Cùng lớp harm với A, khác cơ chế.

## Đính chính TRI THỨC (judge kiểm nguồn Dify, không suy đoán)

Niềm tin "1 trigger/workflow" là **SAI với bản Dify này**: `webhook_service.py:755` và
`workflow_schedule_tasks.py:59` mỗi cái truyền `root_node_id` của trigger tương ứng, `_find_root_node_id`
tôn trọng root được chỉ định ⇒ **webhook + schedule cùng một workflow là hợp lệ**.

Nguồn niềm tin: `.claude/skills/dify-build/spec.md:74` viết *"at most ONE **schedule** trigger per
workflow"* — đúng nghĩa hẹp, nhưng R2-G03 **đọc thành "một trigger tổng cộng"** rồi tách 2 file
(chính là ca sinh ra finding A). ⇒ Guidance **mơ hồ, cần làm rõ** (spec 074 S5).

## Bug build đáng ghi (judge tìm)

- **G02**: code node khai `outputs.diem.type: number` nhưng có 4 nhánh `return None` → **chính đường
  dự phòng có thể giết cả run**, mất luôn phần nhận xét. Ironic và thật.
- **G03**: aggregate `if dt is not None and dt < cutoff: continue` → dòng **thiếu ngày bị đếm lại mãi
  mãi** (field `occurred_at` là optional). Biến thể của bẫy R3-G03 ⇒ **n=2 cho họ "lọc cửa sổ thời
  gian dễ sai"** — ứng viên guidance.
- **G02** (prompt công bằng): cấm tuổi/giới/trường nhưng **hở "năm tốt nghiệp"** — proxy tuổi kinh
  điển trên CV Việt.

## Đạt đáng ghi nhận

G01: biên `<=` **đúng** (bẫy user dặn kỹ nhất) + **0 node gửi mail** · G02: ràng buộc công bằng nằm
**trong system prompt**, cấm cả hai tầng (ảnh hưởng điểm + nhắc trong nhận xét), **không nhánh tự
loại** · G03: `Asia/Tokyo` tường minh, sort giảm dần, 【至急】 có điều kiện, **hai nhịp đều làm và
tuyên bố rõ** — không bỏ nhịp nào.

## Sổ sau 6 đợt → xem [spec 074](../../specs/074-campaign-findings-consolidated-fix.md)
