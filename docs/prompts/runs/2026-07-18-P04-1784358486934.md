# P04 — Inquiry form routing (webhook) · run 1784358486934 · 2026-07-18

**Verdict: PASS chất lượng, ③ CHẬM (522s) — lỗ 071 TÁI HIỆN (ca thứ 2).** Build đúng nghiệp vụ
(webhook → phân loại 4 lớp → 4 room Chatwork, その他 làm default, truyền nguyên văn field). Nhưng ③
lại thrash 522s tìm ví dụ `trigger-webhook` — **cùng chữ ký với P11**, xác nhận không phải ngẫu nhiên.

## Prompt (nguyên văn)
```
会社の問い合わせフォーム（Googleフォーム）に来た内容を、「営業」「サポート」「採用」「その他」に
自動で仕分けして、それぞれの担当のChatworkルームに転送したいです。フォームが送信された
タイミングで自動で動いてほしいです。判断に迷うものは「その他」に入れて、人が見る運用にします。
転送するときは、元の問い合わせ文とフォームに入っていた会社名・名前・メールアドレスも一緒に
送ってください。
```

## Chạy
| | |
|---|---|
| taskId | 1784358486934 |
| fired_at | 2026-07-18 16:08:06 +09 |
| kết cục | status=done · phase=④ · error=none |
| tổng thời gian | **839.0s** (analyze 60.4 · spec 198.9 · **implement 522.1** · test 57.6) |
| build | mode=workflow · 10 node · entry `trigger-webhook` · 1 if-else · **4 http POST** (4 room) |

## Chấm theo checklist P04

| Điểm chấm | Kết quả |
|---|---|
| Webhook trigger cho "フォーム送信タイミング"? | ✅ entry `trigger-webhook`. digest nói rõ "フォーム送信をきっかけに". |
| Phân loại 4 lớp + その他 default khi mơ hồ? | ✅ digest: "判断に迷うものは『その他』へ寄せる" — đúng lệnh user. |
| Route 4 nhánh → 4 room Chatwork? | ✅ 4 http-request POST (mỗi room 1 cái). |
| Truyền nguyên văn field (会社名/名前/メール)? | ✅ 33 tham chiếu field/その他 trong YAML — dữ liệu gốc đi kèm. |
| comprehension (0 jargon)? | ✅ PASS. |

## ⚠️ Lỗ 071 TÁI HIỆN — giờ có 2 mẫu

**③ implement: 522.1s · 60 call · 12 lỗi · 7 grep/find bị chặn** — `--dump-schema` gọi 4 lần (đúng
việc). Nhưng **cả 7 lệnh bị chặn đều tìm `trigger-webhook` example** (`grep -rln "trigger-webhook" .`,
`find … -name`, `grep "type: trigger-webhook" --include=*.yml`).

Đối chiếu 2 run webhook:

| run | prompt | ③ time | grep denied | tìm gì |
|---|---|---|---|---|
| 1784357457370 | P11 phone | 490.8s | 17 | webhook example (7) + STT plugin (10) |
| 1784358486934 | P04 form | 522.1s | 7 | webhook example (7) |

**Kết luận cứng**: mọi build `trigger-webhook` đều tốn ~500s ở ③ vì **repo chưa có pattern webhook**
(cả 10 pattern chỉ có `trigger-schedule`). Agent gõ đúng `find.py --has trigger-webhook` → im lặng →
grep → bị chặn. Đây CHÍNH XÁC là spec 071 S1 (promote `webhook-per-row-notify.yml`) + S3 (feature key
`has_trigger_webhook`) + S4 (`find.py` báo "feature không tồn tại" thay vì im lặng). **3 build P04
này + P11 + build spec-071 gốc = nguồn promote sẵn sàng.**

## MANUAL còn nợ
- Gắn Apps Script vào Google Form thật để POST tới webhook URL Dify (Form không tự gọi webhook —
  digest có nói cần cấu hình phía Form không? cần đọc kỹ).
- 4 room id + token · bật trigger · gửi thử 4 loại inquiry xem route đúng + その他 fallback.
