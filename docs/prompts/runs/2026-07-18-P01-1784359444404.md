# P01 — News pipeline → WordPress (ca gốc stakeholder, scope lớn nhất) · run 1784359444404 · 2026-07-18

**Verdict: PASS mạnh** — ca khó nhất về *honesty phạm vi* (user hỏi thẳng 「全部できますか？無理な
部分は？」), và Builder trả lời đúng cách: chọn một lát dọc khả thi, nêu rõ 5 phần khó/không làm được,
điền plugin hash THẬT (không bịa, không để rỗng). **Cũng là đối chứng hoàn hảo cho lỗ 071.**

## Prompt (nguyên văn)
```
社内でAI・DX系のニュースをまとめてWordPressに載せる作業を自動化したいです。今は人手で…
・Chatworkに共有されたニュースのスクショ画像から、タイトル・URL・概要をOCRで抜き出す
・AI・DX関連のキーワードで定期的にWeb記事やRSSを収集して…
・画像分とWeb分をマージ、URL重複除去、ソース元も分かるように
・関連度と新しさでスコアリング→優先順に「検討リスト」
・各ニュースの記事初稿（タイトル・リード文・本文・タグ案）をLLMで生成
・WordPress投稿用HTMLに変換
・生成HTML・初稿をGoogleスプレッドシート/ドキュメントに書き込む
これ、全部まとめてDifyでできますか？無理な部分があるなら、できる範囲で組んで、
できない部分は何が必要か教えてください。
```
（全文は run dir に）

## Chạy
| | |
|---|---|
| taskId | 1784359444404 · fired 2026-07-18 16:24:04 +09 |
| kết cục | status=done · phase=④ · error=none |
| tổng thời gian | **655.8s** (analyze 79.4 · spec 221.7 · implement 320.8 · test 33.9) |
| build | 12 node · entry `trigger-schedule` · code/http/iteration/llm/**tool** |

## Chấm theo checklist P01

| Điểm chấm | Kết quả |
|---|---|
| Không nhồi 7 tầng vào 1 flow — chọn lát dọc + nói rõ? | ✅ chọn nhánh **thu thập định kỳ** (schedule), **tách** phần ảnh Chatwork on-demand ra và **nêu lý do** (open Q1: schedule không nhận file lúc chạy). |
| Trả lời thẳng 「無理な部分は？」 với 5 điểm cụ thể? | ✅ open questions nêu đúng: (1) ảnh Chatwork cần manual/external, (2) web全文検索 cần search API plugin, (3) Google Docs có thể chưa có tool→dùng Spreadsheet, (4) **timezone UTC→Asia/Tokyo**, (5) LLM件数上限 cần thiết kế. |
| OCR = LLM vision, nói thật (không bịa OCR plugin)? | ✅ nêu ảnh Chatwork ngoài luồng schedule — không giả có OCR. |
| Tool node (Sheets): hash THẬT, không bịa/rỗng? | ✅ **2 dependency có hash đầy đủ**: `langgenius/google:0.1.5@cbe3d3ef…` + `omluc/google_sheets:0.0.2@17f06eaa…`. `lint_plugin_hashes: 0`. **Không** `dependencies: []` (tránh lỗi 067). |
| Note liệt kê tool + hướng dẫn cài? | ✅ "uses GoogleSearch, Batch Update. (1) install from Marketplace (2) add API key (3) test". |
| comprehension (0 jargon)? | ✅ PASS. |

## ⭐ Đối chứng cho lỗ 071 — schedule (có pattern) vs webhook (không pattern)

P01 dùng `trigger-schedule` (**CÓ** pattern `scheduled-fetch-notify`/`scheduled-tool-append`) và ③
chỉ **21 call · 4 lỗi · 2 grep denied · 320.8s** — dù build này **phức tạp hơn** P04/P11 (12 node,
iteration + tool node + 2 plugin hash phải resolve). So sánh cùng đợt, cùng độ phức tạp:

| run | entry | pattern có sẵn? | ③ call | grep denied | ③ time |
|---|---|---|---|---|---|
| **P01** news | trigger-schedule | ✅ có | **21** | **2** | **320.8s** |
| P04 form | trigger-webhook | ❌ không | 60 | 7 | 522.1s |
| P11 phone | trigger-webhook | ❌ không | 96 | 17 | 490.8s |

**Kết luận A/B**: khác biệt DUY NHẤT có ý nghĩa là *entry có pattern hay không*. Build phức tạp NHẤT
(P01) lại NHANH nhất ở ③ vì có ví dụ dẫn đường. Đây là bằng chứng đối chứng cho spec 071 S1 (thiếu
pattern webhook), mạnh hơn cả 2 mẫu webhook đơn lẻ.

## MANUAL còn nợ
- **Verify plugin hash THẬT**: `lint_plugin_hashes` chỉ check FORMAT — hash có resolve được từ
  marketplace không cần thử import thật (nếu bịa, Dify báo lỗi plugin khi import, không silent).
- Cài GoogleSearch + Sheets plugin, API key, SPREADSHEET_ID · model vision cho phần ảnh (nếu làm) ·
  bật trigger · chạy thử xem scoring/初稿 có hợp không · robots/ToS khi scraping.
- Quyết phần ảnh Chatwork: làm flow thứ 2 (manual upload) hay bỏ.
