# R3-G02 — Tin tuyển dụng tự-rà từ phân biệt (VI)

**Prompt**: [gen/2026-07-20-ops-mix/G02](../gen/2026-07-20-ops-mix/G02-vi-job-post-compliance.md) · **Run**: `1784547154993`
**Model**: ①③ opus-4-8[1m], ② haiku — ⚠️ trộn model, không so cost · ①4t ②9t ③12t · denied≈ 1/2/1
**Lint 4/4 sạch** · out `projects/_drafts/b_n_m_nh/workflows/main.yml`

## Judge (context sạch): PARTIAL nghiêng PASS

4/4 yêu cầu có mặt đúng chỗ. Kiến trúc chuỗi rà là **điểm sáng**: BA node LLM tách vai —
soạn → làm-sạch (tiêu chí 3 nhóm + ví dụ nói vòng 「trẻ trung」「ngoại hình ưa nhìn」「cao trên
1m65」) → **diff hai bản để lập danh sách đã-sửa** (danh sách là diff thật, không phải LLM tự
khai); ca không-vi-phạm có câu tường minh "Không phát hiện…", cấm bịa thay đổi. End trả 2 biến
tách `job_posting` / `revision_notes` ✅. Digest + SPEC + YAML **thuần Việt** ✅.

**Trừ:**
1. **Notes ④ tiếng Anh** (nợ localize — mẫu thứ 5 và 6 sẽ đếm ở SUMMARY), lặp "an AI model — add
   one" ×3 lần đọc vụng.
2. 3 node LLM `provider: ''` — Model-not-exist sau import (nợ đã biết).
3. **Lỗ tinh vi judge phát hiện** (bẫy tôi không gài được hay đến thế): node-1 có chỉ thị "yêu cầu
   chỉ dựa năng lực" → có thể **tự lược vi phạm ngay khâu soạn** → node-2 hết việc → danh sách
   đã-sửa báo "không phát hiện" — **user mất đúng bài học họ xin**. Chỉ khẳng định được khi chạy
   với JD cài lỗi.

## MANUAL còn nợ

JD thô cài 3 lỗi (「dưới 30 tuổi」「nữ, ngoại hình khá」「nam ưu tiên」) xem: bắt đủ? danh sách
đã-sửa có phản ánh — hay bị node-1 nuốt trước? · biến thể nói vòng lạ ("môi trường năng động trẻ").

**Verdict: PARTIAL (nghiêng PASS)** · slug `b_n_m_nh` ("Bên mình…" lột dấu) = mẫu slug-rác VI thứ 2.
