# P02 — Chatwork screenshot OCR → CSV · run 1784375098683 · 2026-07-18

**Verdict: PASS mạnh** — trục ẢNH (vision LLM thay OCR plugin, file-list input) chưa ca nào chạm,
xử lý đúng toàn bộ: vision enabled, iteration per-ảnh, CSV ghép trong code, và **cấm bịa URL** đúng
như user dặn.

## Prompt (nguyên văn)
```
Chatworkに毎日ニュース記事のスクリーンショットが10枚くらい投稿されます。その画像から
「記事タイトル」「URL」「概要（2〜3行）」だけ抜き出して、コピペで表に貼れるような
カンマ区切りのテキストにまとめてほしいです。画像はこちらでまとめてアップロードする想定で
いいです。URLが画像に写っていないときは空欄でいいです。
```

## Chạy
| | |
|---|---|
| taskId | 1784375098683 · fired 2026-07-18 20:44:58 +09 |
| kết cục | done · ④ · error=none · model=haiku (mọi phase) |
| ③ | 15 turn · **0 grep-denied** |
| build | mode=workflow · 6 node: start · llm · iteration · code · end |

## Chấm theo checklist P02

| Điểm chấm | Kết quả |
|---|---|
| File input **kiểu ảnh, nhiều file**? | ✅ `start.variables = [images: file-list]` |
| Vision LLM (đọc ảnh, không OCR plugin)? | ✅ **`llm.vision.enabled = true`** — đúng cách (Dify không có OCR plugin) |
| Iteration per-ảnh? | ✅ iteration + iteration-start, LLM đọc 1 ảnh/lần |
| CSV ghép trong **code** (không nhờ LLM)? | ✅ code node (36 ref csv/join) |
| **Cấm bịa URL** (user: "空欄でいい")? | ✅ prompt system: 「URLが写っていない場合は必ず空文字 ""（**推測・補完しない**）」 — đúng chính xác |
| JSON schema chặt (3 key)? | ✅ prompt cấm thêm key, cấm code fence |
| comprehension (0 jargon)? | ✅ PASS |

## Ghi chú
Trục vision/ảnh build sạch (0 grep-denied) như mọi trục khác. Điểm tinh tế đạt tốt: build **tự thêm
ràng buộc chống-bịa** cho vision LLM (dễ hallucination URL từ ảnh) — không cần user nhắc chi tiết.

## MANUAL còn nợ
- Model phải **vision-capable** (không phải text bất kỳ) — điền trong Dify; verify note có nói rõ không.
- Độ chính xác đọc ảnh thật (JA font nhỏ trong screenshot Chatwork).
- 10 ảnh/lần có vượt giới hạn file của workspace không.
