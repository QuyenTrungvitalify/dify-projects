# R4-G02 — Chatbot tra cứu bảo hành cho CSKH, trích nguồn + không đoán bừa (VI)

**Prompt**: [gen/2026-07-20-lifecycle/G02](../gen/2026-07-20-lifecycle/G02-vi-warranty-chatbot.md) · **Run**: `1784557777080`
**Model**: ①② haiku, ③ opus-4-8[1m] · ①6t ②7t ③12t · denied≈ 1/1/1 · **Lint 4/4 sạch** · 1 file

## Trục chính: KHÔNG bịa dataset id — ĐẠT (điểm mạnh nhất)

`dataset_ids: []` để trống + TODO hướng dẫn user lấy UUID thật từ workspace; SPEC + notes nói thẳng
3 PDF là bước nạp tay ngoài workflow. **Không id giả nào bị nhét** — đúng chuẩn (builder không thể
biết dataset workspace-local). node knowledge-retrieval trỏ đúng `query_variable`, top_k 4,
reranking tắt nhất quán. Cùng họ với P06 (không bịa dataset) — propensity giữ, mẫu thứ 2.

RAG grounding ✅ · trích `[Nguồn: <tên tài liệu>]` + `end` xuất `context` ✅ · cụm "không có thông
tin" + nhắc chuyển trưởng nhóm ✅.

## 2 finding mới

| # | Finding | Bằng chứng |
|---|---|---|
| **H** | User xin "**con chatbot** tra nhanh" (hội thoại) → build ra `mode: workflow` (form 1-vào-1-ra), **không phải chatflow/advanced-chat** — không giao diện chat, không hỏi-đáp nối tiếp. ① không nhận ra ý định hội thoại → chatflow | `app.mode: workflow`, `memory: null` |
| **I** | **Nội dung VI sinh ra bị MẤT DẤU**: prompt buộc model xuất `"khong co thong tin trong tai lieu"` trong khi user đòi nguyên văn CÓ DẤU "không có thông tin trong tài liệu" → model trả cụm sai literal. Lưu ý: **digest lại có dấu đúng** — mất dấu chỉ ở *thân YAML sinh ra* (prompt/description) | grep `khong co thong tin` trong main.yml |

Notes ④ tiếng Anh (finding B tiếp diễn).

## Cần chạy thật

Nạp 3 PDF + gắn dataset_ids + chọn model → hỏi vài câu (gồm 1 câu ngoài tài liệu) xem bám nguồn +
trả đúng cụm fallback không.

**Verdict: PARTIAL** — lõi chống-bịa đạt; trừ vì mode sai + mất dấu VI. slug `b_n_m_nh_2` (VI, thứ 3).
