# P05 — Invoice reconcile → dunning draft · run 1784375623443 · 2026-07-18

**Verdict: ❌ DEFECT — ② không tạo SPEC.md ở auto mode (bug thật đầu tiên của loạt test).** Không
phải quota (session-limit=0). Build không hoàn thành. Gốc rễ đã xác định + có bản vá đề xuất.

## Prompt (nguyên văn)
```
毎月、経理で「請求一覧」と「入金一覧」の2つのExcelを目視で突き合わせて、まだ入金されていない
会社を洗い出して、1社ずつ督促メールの下書きを作っています。この2つのファイルをアップロード
したら、・未入金の会社リスト（会社名・請求番号・金額・期日）・各社向けの督促メール文面
（丁寧なトーンで）を出してくれるようにできますか？金額と会社名は絶対に間違えないでほしいです。
メールは自動送信しないでください、下書きだけでいいです。
```

## Chạy
| | |
|---|---|
| taskId | 1784375623443 · fired 2026-07-18 20:53:43 +09 |
| kết cục | **status=error · phase=② spec · "artifact missing: SPEC.md"** |
| quota? | **không** — session-limit = 0 trong transcript |

## Bug: ② hỏi thay vì tự-giả-định-và-viết (auto mode)

Ở mode `auto` (không dừng gate), ② lẽ ra tự giả định phần mơ hồ + viết SPEC.md + tiến. Nhưng LLM ②
**viết một câu hỏi người dùng vào chat** thay vì tạo file, và còn tưởng đã viết:

> 「先ほどの仕様（SPEC.md）を書き終えて、確認待ちの状態で止まっています。この先どう進めるか…
> **このまま実装に進む** → 「実装して」とお伝えください…」

SPEC.md không được tạo → Builder park `error: artifact missing` (**xử lý ĐÚNG** — không giả done).

## Gốc rễ (đã xác định)

`analyze.md` (phase ①) dạy rõ: *"Autonomous modes (auto/spec_only) auto-confirm this gate, so never
make the digest DEPEND on an answer"* → ① luôn giả định + ghi open point + tiến (đã chứng minh qua 8
run). **`spec.md` (phase ②) KHÔNG có hướng dẫn này** (`grep auto-confirm` = 0). Nên khi requirement
nhiều ẩn số (P05: cột Excel nào, khóa đối chiếu nào, định nghĩa 未入金), ② tưởng hỏi sẽ được trả lời →
dừng hỏi → không viết artifact.

**Vì sao P05 kích hoạt còn 8 ca trước không**: P05 có ambiguity CAO (2 file Excel cấu trúc chưa biết,
khóa đối chiếu, định nghĩa nghiệp vụ) — đủ để ② "muốn hỏi". Các prompt trước hoặc rõ hơn, hoặc ① đã
nuốt hết ambiguity.

## Bản vá đề xuất

Thêm vào `spec.md` cùng hướng dẫn auto-confirm mà `analyze.md` đã có: ở auto/spec_only, ② PHẢI giả
định phần mơ hồ (ghi vào **Open questions** của SPEC), viết SPEC.md, và tiến — KHÔNG bao giờ để turn
kết thúc bằng câu hỏi mà không có artifact. Đây là **gap đối xứng** với thứ analyze.md đã vá cho ①.

Rủi ro fix: thấp (1 file prompt, cô lập). Nhưng như S3: propensity, verify bằng retry P05.

## Trạng thái test
P05 CHƯA đánh giá được chất lượng build (2-file reconcile, cấm-LLM-đụng-số, no-auto-send) vì build
dừng ở ②. Cần retry SAU khi vá spec.md.

---

## ⚠️ RETRY (run 1784381510236, 22:31) — DEFECT **KHÔNG LẶP LẠI**

Chạy lại **cùng prompt, cùng mode auto**: ② viết SPEC.md bình thường, build hoàn tất.

| | Lần 1 (1784375623443) | Retry (1784381510236) |
|---|---|---|
| kết cục | ❌ error @ ② artifact missing | ✅ done @ ④ |
| ② hành vi | viết câu hỏi vào chat | viết SPEC.md |

**Chất lượng build retry — đạt điểm khó nhất:**
- `start`: **2 file input** (`invoice_file`, `payment_file` — file-list) ✓
- **4 code node** lo parse + `Match unpaid` + assemble → **phép so tiền/tên do CODE làm** ✓
- **LLM chỉ "Compose draft"** — không đụng vào đối chiếu số ✓ (đúng 「金額と会社名は絶対に間違えない」)
- **0 node http/tool** → không gửi mail ✓ (đúng 「自動送信しないでください」)
- iteration per công ty · document-extractor · 4 linter 0/0/0/0 · comprehension PASS · 0 grep-denied

## Kết luận sửa lại

Đây **KHÔNG phải defect tất định**. Nó là **propensity**: khi requirement nhiều ẩn số, ② *đôi khi*
chọn hỏi thay vì giả định — vì `spec.md` **không cấm** điều đó (`analyze.md` có câu auto-confirm cho
①, `spec.md` không có). Quan sát 1/2 lần.

Gap doc vẫn THẬT và đáng vá, nhưng mô tả trung thực là **giảm xác suất ② dừng hỏi**, không phải "sửa
một bug luôn xảy ra". Và Builder **contain đúng** cả hai lần (park error, không giả done).
