# Campaign v0.2.0 — `2026-07-22-corpus-wide` (đợt 9) · 6 đề · đánh giá theo GÓC USER (tốc độ + chất lượng)

Đối chiếu: [CAMPAIGNS.md](CAMPAIGNS.md) · đề: [gen/2026-07-22-corpus-wide/](../gen/2026-07-22-corpus-wide/)
· đề tài từ corpus Dify thật · report bằng cơ chế 075 (journey + criteria_check).

Đợt RỘNG nhất (6 đề) và **khó nhất** — chạm đúng ca-biên thật, ra 1 finding mạnh + 1 finding process.

## ⏱ TỐC ĐỘ — user chờ bao lâu (journey timeline)

| Đề | ① analyze | ② spec | ③ implement | TỔNG |
|---|---|---|---|---|
| G01 research | 79s | 95s | 115s | **289s** (nhanh nhất) |
| G02 json | 81s | 121s | 200s | 402s |
| G03 chart | 115s | 219s | 199s | 533s (chậm nhất) |
| G04 long-translate | 80s | 166s | 268s | 515s |
| G06 file-brief | 51s | 359s | — | 512s (③ nặng) |
| G05 title | 51s | **② lỗi 137s** | — | **KHÔNG hoàn thành** |

Nhận xét tốc độ (user ngồi xem): mỗi build **5-9 phút**, ③ thường nặng nhất (build YAML). Không build
nào thrash bất thường ngoài G05. Trần **quota chạm ở build 5** của loạt → phải chờ reset (~9h) rồi
resume — đây là ma sát vận hành thật cho loạt lớn một phiên.

## ✅ CHẤT LƯỢNG — judge context sạch, 2 PASS · 3 PARTIAL · 1 lỗi

| Đề (nguồn corpus) | Trục | Judge |
|---|---|---|
| [G01](2026-07-22-R9-G01-1784727336430.md) research (AdvancedSearch) | search+cite honesty | **PASS** |
| [G06](2026-07-22-R9-G06-1784763556095.md) file-brief (simple-kimi) | cấm-suy-đoán 記載なし | **PASS** |
| [G02](2026-07-22-R9-G02-1784727880237.md) json (json_translate) | giữ cấu trúc | **PARTIAL** |
| [G03](2026-07-22-R9-G03-1784728286916.md) chart (chart_demo) | render số | **PARTIAL** |
| [G04](2026-07-22-R9-G04-1784728820870.md) long-translate (BookTranslate) | chia chunk | **PARTIAL** |
| G05 title (TitleCreator) | không-giật-tít | **❌ ② lỗi** |

Đạt mạnh: G01 (google_search thật, URL từ kết quả search, cấm bịa link + fallback「出典が見つかりません」)
· G06 (4 heading, 記載なし gia cố nhiều tầng, cấm bịa số/固有名詞).

## Finding — 1 MẠNH, 2 vừa, 1 process

### 🔴 F1 (mạnh nhất phiên) — trần iteration ≤30 KHÔNG xử lý cho input dài → VỠ ÂM THẦM
G04: chia chunk `max_chars=4000`, `input max_length=200000` → tối đa **50 chunk**; ngay "何十ページ"
thực tế ≈ **34 chunk** → **vượt trần 30** của Dify (`constraints.md §2`: >30 fail runtime KHÔNG lỗi rõ).
Build không clamp/batch về ≤30, không cảnh báo. **Đúng kịch bản user nhấn ("長すぎて切れちゃう") là chỗ
build vỡ.** Khác S1/S2: ở đây yêu cầu-nêu-rõ **KHÔNG** dẫn tới xử lý đúng → nghiêm trọng hơn.
Cộng dồn finding K (translate_llm thiếu max_tokens → chunk dài cắt cụt) + `continue-on-error` (chunk lỗi
rớt im). → **Guidance thật đáng làm**: build iteration trên input độ-dài-user phải clamp ≤30 hoặc cảnh báo.

### F2 (n=1) — biến đổi giữ-cấu-trúc dùng LLM-first, KHÔNG kiểm fidelity cơ học
G02: LLM dịch cả file JSON, code chỉ validate cú pháp (`json.loads`) — **không diff key-set input↔output**,
nên LLM đổi key/dịch URL vẫn `valid:True` (an toàn giả). Ràng buộc "tuyệt đối không đổi key" tựa hoàn
toàn vào prompt. Cùng họ "ràng buộc cứng dựa prompt thay vì code".

### F3 (process, propensity) — ② KHÔNG viết SPEC.md tái hiện trên v0.2.0
G05: phase ② chạy 5 turn/137s (đọc analyze, search find.py) rồi **kết thúc mà chưa viết SPEC.md** →
build chết "artifact missing: SPEC.md". Đây là finding P05 đợt đầu (đã fix v0.2.0 bằng guidance "NEVER
end without writing"). **Fix là nudge, không triệt** → n=1 tái hiện. Đề title-gen đơn giản mà vẫn dính.

### F4 (nuance, không hẳn lỗi) — G03 "画像" = echarts-in-chat, không phải file PNG
Build honest (SPEC nêu open question, mặc định render-in-chat), số parse bằng code tất định. Lệch chữ
"画像" nhưng "パッと見でわかる" đạt.

## Tổng 9 đợt (39 đề máy-sinh + 12 tay)
Corpus-grounding hiệu quả: đề khó thật, tìm ra **finding iteration-cap** (F1) — actionable nhất từ trước
tới nay. Còn ~19 workflow corpus chưa khai thác. Cơ chế report 075 chạy đủ (5/5 build done có
criteria_check + timeline; journey cho bảng tốc độ trên).
