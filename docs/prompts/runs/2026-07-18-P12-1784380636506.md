# P12 — Edit-existing translation flow · run 1784380636506 · 2026-07-18

**Verdict: ⚠️ Build ĐÚNG chức năng, nhưng lộ 2 vấn đề** — ① digest ra **tiếng Anh** cho prompt JA
(lang-sync fail), và trục edit-existing **không test được qua harness** (thiếu `--workflow`).

## Prompt (nguyên văn)
```
前に作ってもらった翻訳のフローに機能を足したいです。訳した英語の文字数もカウントして出して
ほしいのと、もし300文字を超えたら、意味を変えずに短くした案も一緒に出すようにしてほしいです。
元の翻訳の動きは変えないでください。
```

## Chạy
| | |
|---|---|
| taskId | 1784380636506 · fired 2026-07-18 22:17:16 +09 |
| kết cục | done · ④ · error=none |
| base | **`workflow: none · seed: none`** — KHÔNG nhận base (build from-scratch) |
| build | 7 node: start · llm ×2 · code · if-else · variable-aggregator · end · 4 linter 0/0/0/0 |

## Chất lượng chức năng: ĐẠT

Shape đúng yêu cầu: dịch → **code** đếm ký tự (không nhờ LLM đếm — đúng) → **if-else** >300 → LLM rút
gọn → aggregator → end. 4 linter sạch.

## ⚠️ Vấn đề 1 — ① digest ra TIẾNG ANH cho prompt JA (lang-sync fail)

`analyze.json.overview`:
> *"Extend an existing translation workflow: after producing the English translation, also report its
> character count…"*

Prompt là **tiếng Nhật thuần**. Spec 030 (languagePin) buộc digest theo ngôn ngữ requirement.
**Đối chứng cùng đợt**: P02 (JA) → overview JA đúng 「ユーザーがまとめてアップロードした…」.
**Cô lập**: ② SPEC.md của CHÍNH run này lại **tiếng Nhật đúng** (`# 翻訳＋文字数カウント…`) → chỉ ①
lệch, không phải toàn build.

Giả thuyết: prompt P12 nói về "flow đã có" + nội dung là dịch **sang tiếng Anh** → có thể đẩy ① sang
EN. Cần lặp lại để xác nhận (n=1).

## ⚠️ Vấn đề 2 — trục edit-existing KHÔNG test được qua harness

`e2e-run.sh fire` chỉ có `--project`, **không có `--workflow`** để chọn base. Nên dù `_drafts` có sẵn
`workflow_2` (chính build P08: 翻訳＋用語集 — base lý tưởng), builder nhận `workflow: none` và build
mới. Đây là **hạn chế harness**, không phải bug builder (suite entry `edit-existing` đã ghi:
"Requires a base workflow… OQ2 — --project only").

Hệ quả phụ đáng chú ý: digest nói *"Extend an existing translation workflow"* trong khi thực tế
**không có base nào** — ① không nêu rằng nó không biết "flow trước" là flow nào (không có open
question về base). Với người dùng thật, đây là im lặng đáng ngại: họ tưởng đang mở rộng flow cũ.

## MANUAL còn nợ
- Test edit-existing THẬT: qua UI (chọn workflow) hoặc thêm `--workflow` vào harness.
- So diff với base: phần cũ có bị đụng không (yêu cầu 「元の動きは変えない」).
- 300 **ký tự** hay **từ**? digest hiểu là ký tự — đúng 文字数, nhưng đáng xác nhận với user.
