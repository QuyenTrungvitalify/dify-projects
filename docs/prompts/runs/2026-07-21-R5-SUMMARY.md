# Campaign v0.2.0 — `2026-07-21-capabilities` (đợt 5) · 3 trục CHƯA từng chạm

Đối chiếu: [CAMPAIGNS.md](CAMPAIGNS.md) · đề: [gen/2026-07-21-capabilities/](../gen/2026-07-21-capabilities/)
· đợt máy-sinh thứ 4, chọn axis mới để mỗi build thêm tín hiệu thay vì lặp finding chín.

## Kết quả

| Đề | task | Trục mới | Judge |
|---|---|---|---|
| G01 review-batch (JA) | 1784598243240 | iteration/mảng động 5–50 | **PARTIAL** |
| G02 onboarding chatflow (VI) | 1784598542109 | chatflow nhớ ngữ cảnh (mẫu 2 finding H) | **PASS** |
| G03 outbound-voice (JA) | 1784598916554 | từ chối trung thực voice/IVR | **PASS** |

3/3 done · lint sạch · 1-file cả ba · 0 fail hạ tầng.

## Hai kết luận TINH CHỈNH (chỉ n=2 mới thấy — biện minh cho đợt axis-mới)

- **Finding H (chatbot→mode) = PROPENSITY theo độ mạnh tín hiệu, KHÔNG phải bug.** R4 G02 tín hiệu
  yếu ("một con chatbot tra nhanh") → `mode: workflow`; R5 G02 tín hiệu mạnh (mô tả follow-up tường
  minh) → `advanced-chat` + memory 2 node + answer + **query-rewrite node** (vượt mức tối thiểu).
  → **KHÔNG fix bằng 1 dòng code**; nếu muốn cải thiện là guidance ① nhận diện ý định hội thoại.
- **Finding I (mất dấu VI) = KHÔNG tất định.** R4 G02 mất dấu ("khong co"); R5 G02 **đủ dấu hoàn
  toàn** ("không có"). n=1 lỗi / n=1 sạch → **chưa đủ mẫu, đừng fix vội** (đúng luật n=1).

Đây là giá trị lõi của việc thêm mẫu: **2 finding tưởng chín hóa ra không phải bug cơ học** — sổ
tích lũy vừa cứu ta khỏi fix nhầm.

## Finding mới

| # | Finding | Bằng chứng | Nhãn |
|---|---|---|---|
| **K** | Single-prompt-over-iteration là lựa chọn ĐÚNG (top-3 cần gom xuyên suốt; né trần iteration 30) NHƯNG không hardened cho N lớn: **thiếu `max_tokens`** (rủi ro cắt output ở 50 review), không guard đếm dòng == count, không cảnh báo giới hạn cho user | G01 | n=1 — guidance implement.md |

## Giá trị LỚN NHẤT đợt: chạy thật bắt 2 BUG HARNESS mà stub không thấy

| Bug | Gốc | Fix |
|---|---|---|
| Runner **abort ở turn-lock 409** thay vì chờ | `if ! fire_one; then RC=$?` — `!` nuốt exit code → RC luôn 0 → nhánh busy dead code | capture `RC` trực tiếp + `CAMPAIGN_BUSY_WAIT` + drill mới. **Fix xác nhận sống**: resume đợt này gặp lock, chờ, settled |
| `record` **không idempotent** → G01 thành `[X,X]` | append mù (double-launch của tôi) | no-op khi task_id đã có + test |

Cả hai chỉ lộ khi chạy thật với orphan-build giữ lock — S5 drill chỉ có `fire exit 0`. Bài học:
**stub drill không thay được một lần chạy thật ở nhánh lỗi.**

## Trục đều ĐẠT — không lỗi chất lượng lõi

- iteration: G01 chọn single-prompt đúng lý do; G03 **dùng iteration thật** (vòng từng khách +
  continue-on-error) — trục được chạm ở G03.
- chatflow: G02 mẫu chatflow đúng-bài nhất từng ghi (query-rewrite trước retrieval).
- honesty: G03 dùng `example.invalid` + TODO, không giả gọi điện, nói thẳng DTMF cần flow-2. Không
  bịa dataset (G02). Cùng họ P11 (từ chối STT) — honesty giữ qua domain mới.

## Sổ tích lũy sau 5 đợt (15 build máy-sinh) — CHỐT cho 1-lần-fix

| Finding | Loại | Mẫu | Fix? |
|---|---|---|---|
| **B** notes-EN cho JA/VI | cơ học (template chưa localize) | ~mọi build | **SẴN — fix thật** |
| **8** slug rác (JA+VI) | cơ học | n=8 | **SẴN — fix thật** |
| **A** ④ giả định 1-file | cơ học | n=1 | Builder-side (harness đã vá) |
| **7** đường tra tri thức | thiếu năng lực | n=4 | cần quyết mở rộng |
| **E** fail-open duyệt tiền · **K** thiếu max_tokens N lớn | guidance | n=1 mỗi | implement.md |
| **H** chatbot→mode · **I** mất dấu VI · **J**/notes-mỏng | **propensity/không tất định** | n=2 | **KHÔNG fix cơ học** |

**Khuyến nghị dừng test ở đây**: 5 đợt đã tách bạch rõ *bug cơ học* (B, 8 — fix ngay) khỏi
*propensity* (H, I — đừng fix). Test thêm cùng version = lợi tức giảm. Bước đúng: **user fix B + 8**,
rồi `recheck` cả 5 đợt.
