# Campaign v0.2.0 — `2026-07-20-quiz-gen` · 3 đề sinh máy, đợt nghiệm thu harness `/campaign`

Đối chiếu: [CAMPAIGNS.md](CAMPAIGNS.md) · đề đóng băng: [gen/2026-07-20-quiz-gen/](../gen/2026-07-20-quiz-gen/)
· harness: spec 073 (lần chạy thật đầu tiên).

**Mục tiêu kép**: (1) test Builder v0.2.0 trên chủ đề "sinh trắc nghiệm + lưu/tổng hợp kết quả";
(2) nghiệm thu chính `/campaign`. **Mode** `auto` · deploy none · gate duyệt đề được user ủy quyền
trong phiên.

## Kết quả

| Đề | Trục | ③ denied | Judge (context sạch) |
|---|---|---|---|
| [G01](2026-07-20-G01-1784522395970.md) PDF→20 câu→Sheets | catalog-TRÚNG + cân bằng đáp án | 11 | **PASS** (bảo lưu) |
| [G02](2026-07-20-G02-1784524145378.md) VI 3 mức, tách đề/đáp án | lang-sync VI + tách kênh | 5 | **PARTIAL** |
| [G03](2026-07-20-G03-1784524641184.md) webhook điểm + tổng hợp tháng | advisory nguồn + đàm phán 2-nhịp | 8 | **PARTIAL** |

3/3 hoàn thành không cần retry · 4 linter sạch (main.yml) · comprehension 3/3 · 0 fail hạ tầng.

## Trục chủ đích: 4 ĐẠT đáng ghi

- **Đường catalog-TRÚNG lần đầu có mẫu** (G01): hash `omluc/google_sheets` khớp catalog từng ký tự
  — resolve offline, không bịa, không hạ cấp http-request.
- **Advisory nguồn webhook (v0.2.0) bắn đúng field** (G03) → mẫu sống #1/≥2 cho gate hồi quy (mục 9).
- **Đàm phán phạm vi tốt nhất từng ghi nhận** (G03 ①): tự khai đủ 5 bẫy, tách 2 workflow đúng luật.
- **Tách kênh đề/đáp án bằng kiến trúc** (G02): judge soi mọi nhánh lệch shape, không thấy đường rò code.

## Finding mới

| # | Finding | Bằng chứng | Nhãn mẫu |
|---|---|---|---|
| **A** | **Tầng ④ giả định MỘT file/workflow**: build đa-file → file thứ 2 không qua linter, notes im lặng → user triển khai nửa hệ thống mà không biết. `campaign.py record` cũng chỉ gặt 1 `workflow_file` — giả định lan sang harness | G03: `monthly_summary.yml` tồn tại, `report.json` không biết | n=1, **lớp lỗi hệ thống** — ưu tiên cao nhất đợt |
| **B** | **Notes ④ tiếng Anh cho persona JA/VI, chứa chỉ dẫn sống còn** ("add an AI model") — nợ localize NOTE_JA/VI đã biết (spec 063 EN-first), giờ có bằng chứng harm: user không đọc được đúng câu khiến workflow chạy được. Lưu ý: comprehension PASS 3/3 *chính vì* nó chỉ quét bản EN | G01+G02+G03 (3/3 build) | n=3 trong đợt — đủ mẫu nâng ưu tiên nợ |
| **C** | SPEC hứa (model plugin anthropic) → Implement không giữ (`dependencies: []`, model rỗng) | G02 | n=1 |
| **D** | Build-level: cân bằng `i%4` → pattern đáp án đoán được; aggregate G03 không lọc tháng (sai từ tháng 2); append rủi ro đè A1 | G01, G03 | bug của build sample — chỉ bắt được bằng judge/chạy thật, linter mù |

## Finding cũ — thêm mẫu

- **Slug rác (mục 8)**: `pdf_4_20_1` · **`t_i_ph_tr`** (VI đầu tiên — lột dấu "Tôi phụ trách") ·
  `google_1_80_chatwork` → n=3 mẫu mới, **không còn là chuyện riêng JA — đủ mẫu để fix**.
- **Lang-sync (mục 6)**: G02 digest VI đúng = mẫu đúng thứ 2; mẫu sai vẫn n=1 (P12-EN), chưa tái hiện.
- **Model không pin**: G01 ①② nhảy opus — mọi so sánh cost đợt này chỉ hợp lệ nội-G03 (haiku trọn).

## Nghiệm thu harness `/campaign` (mục tiêu 2)

**ĐẠT** — trọn vòng plan→lint→gate→freeze→run→record→report chạy thật không hỏng khâu nào; runner
3/3 không cần retry, quota không chạm. Lỗi harness tự bắt trong đợt: `#N` trần trong YAML bị
round-trip nuốt thành comment (đã fix bằng quote); skill registry cần restart phiên mới nhận
`/campaign` (chạy thủ tục trực tiếp vẫn OK); finding A chạm cả `campaign.py record` (1-file).

## Việc để lại cho người (không tự fix theo phân vai)

Fix theo thứ tự đề nghị: **A** (④ đa-file — sửa report.ts/linters quét mọi `workflows/*.yml` của
build) → **B** (trả nợ NOTE_JA/VI localize) → slug (mục 8, giờ đủ mẫu) → C. Fix xong chạy
`/campaign recheck 2026-07-20-quiz-gen` để có bảng trước/sau cùng-đề.
