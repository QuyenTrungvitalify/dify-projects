# R7-G01 — Ghi dồn đơn vào Sheets (JA) · verify S1

**Prompt**: [G01](../gen/2026-07-22-data-integrity/G01-daily-log-accumulate.md) · **Run**: `1784713103022` (`1_google_1_2`)
**Report 075**: criteria_check 6 mục (đều manual) · timeline ①100s ②208s ③182s = **489s** · haiku cả 3

## Journey (cơ chế mới)
① digest → ② build **tự cam kết** "追記 dưới dòng cuối, KHÔNG ghi đè" (criteria #2) + "date thiếu → JST
hôm nay" (#4) → ③ node `webhook_in → read_tail → build_row → append` → ④ notes 1503 ký tự.

## S1 verify (tĩnh): build ĐÚNG
```
rng = sheet + "!A" + str(existing + 1)   # đọc existing từ batch_get → ghi dòng KẾ
```
2 batch_get, không range A:D cứng. Yêu cầu nhấn "đừng mất dòng cũ" → builder append chuẩn.
→ **S1 là propensity theo độ nhấn yêu cầu, không phải bug hệ thống.**

## Còn nợ runtime
Plugin batch_update với range `A{n+1}` có ghi đúng dòng đó trên Dify thật không — cần chạy live.

**Verdict: PASS (tĩnh)** — append logic đúng; model rỗng (nợ); runtime cần verify.
