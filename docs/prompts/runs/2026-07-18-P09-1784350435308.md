# P09 — Vague sales email · run 1784350435308 · 2026-07-18

**Verdict: PASS (mạnh)** — ca test sự-mơ-hồ, và Builder xử lý đúng cách: biến 2 câu mù mờ thành
digest rõ + 3 câu hỏi mở + mặc định an toàn, build tối giản, **không** thêm side-effect nào.

## Prompt (nguyên văn)
```
営業メールの返信、だいたい毎回同じようなことしか書いてないので自動化したいです。
いい感じにお願いします。
```

## Chạy
| | |
|---|---|
| taskId | 1784350435308 |
| fired_at | 2026-07-18 13:53:55 +09 |
| kết cục | status=done · phase=④ · error=none |
| tổng thời gian | **322.8s** (analyze 75.1 · spec 112.0 · implement 109.8 · test 25.9) |
| turns | ① 7 · ② 6 · ③ 15 · tổng ~28 · cache 97–98% |
| pattern | `custom` (đúng — không mẫu nào khớp "sinh 1 email") |
| build | mode=workflow · 3 node `start → llm → end` |
| deploy | none |

## Chấm theo checklist P09

| Điểm chấm | Kết quả |
|---|---|
| ① biến mơ hồ → giả định rõ + câu hỏi mở? | ✅ **xuất sắc.** 3 open question đúng chỗ: (1) "定型的な内容" gồm gì, (2) khởi động thủ công hay trigger — **mặc định thủ công**, (3) input dạng gì — mặc định dán text. Đây đúng là "biến mơ hồ thành giả định an toàn". |
| Không tự thêm side-effect user chưa xin? | ✅ **0 node http/tool/gửi-mail.** Không tự ý thêm phân loại/CRM/auto-send. |
| Build nhỏ đúng mức (không over-build)? | ✅ 3 node. Prompt xin 1 việc → build 1 việc. |
| digest đúng ngôn ngữ (JA)? | ✅ toàn JA từ token đầu. |
| comprehension (0 jargon)? | ✅ PASS 0 jargon. |
| note trung thực? | ✅ nêu cần thêm model; chỉ đúng file + cách import. |

## Lỗi / bất thường
- **Một sự cố hạ tầng trước khi chạy** (không phải lỗi build): lần fire đầu `HTTP 000` — server
  builder chưa chạy. Khởi động lại từ `dist` (17:13) rồi fire lại OK. Không ảnh hưởng kết quả.
- Slug project = `workflow` (generic) — vì prompt không gợi tên; chấp nhận được cho từ-đầu.

## MANUAL còn nợ (harness không tự kiểm được)
- Thêm model vào Dify rồi dán 2 email thật → nháp có "いい感じ" theo chuẩn viết mail JA không.
- Đọc digest bằng con mắt chính người viết prompt: có dám giao cho họ duyệt không (spec 063).
