# R9-G04 · Run `1784728820870` · PARTIAL

**Prompt**: [gen/2026-07-22-corpus-wide/G04-long-manual-translate.md](../gen/2026-07-22-corpus-wide/G04-long-manual-translate.md) · report 075: criteria_check + timeline
**Judge (context sạch): PARTIAL**

**F1 (finding mạnh nhất phiên)**: trần iteration ≤30 KHÔNG xử lý — max_chars=4000, input 200000 → tối đa 50 chunk; '何十ページ' thực tế ≈34 chunk → VƯỢT 30 (constraints §2: >30 fail runtime im). Build không clamp/cảnh báo → đúng kịch bản user nhấn là chỗ vỡ. Cộng finding K (translate_llm thiếu max_tokens → cắt cụt) + continue-on-error (chunk lỗi rớt im). ĐIỂM MẠNH: glossary chạy 1 lần trước iteration (nhất quán thuật ngữ), prompt cấm tóm tắt, join giữ thứ tự. ①80 ②166 ③268 = 515s.

Đề từ workflow Dify thật (corpus). Model rỗng (nợ đã biết) · chưa chạy live.

## ✅ Recheck sau fix F1+K (2026-07-22, cùng đề, task 1784771160291)

Guidance mới (runtime-supplement §2-supplement + implement.md) đọc live, không rebuild:

| Điểm | CŨ (1784728820870) | MỚI (1784771160291) |
|---|---|---|
| Clamp ≤30 | `max_chars=4000` cố định → tới 50 chunk | `batch = ceil(N/30)` → **chunks ≤30 mọi N** (copy cả comment cảnh báo) |
| max_tokens | 0 (chỉ temperature) | **2 node: 4096, 8192** |

Build sinh gần như nguyên văn công thức guidance. **n=1** (một mẫu, hành vi LLM) — tín hiệu mạnh
guidance ăn, cần vài mẫu nữa mới chốt tất định. Nuance còn lại (chunk quá lớn vượt context window khi
tài liệu cực dài) ngoài phạm vi F1 — softer limit khác với ≤30 hard-fail.
