# R7-G02 — Tổng hợp tuần này từ Sheets (JA) · verify S2

**Prompt**: [G02](../gen/2026-07-22-data-integrity/G02-weekly-window-filter.md) · **Run**: `1784713596997` (`workflow_4`)
**Report 075**: criteria_check 6 mục (đều manual) · timeline tổng **525s** · haiku cả 3

## Journey
② build tự cam kết "今週期間外 + 状況完了 loại, chỉ đếm dòng cần trong tuần" (criteria #4) + "đếm bằng
CODE không LLM" (#3) → ③ code lọc ngày → ④ gửi Slack manager.

## S2 verify (tĩnh): build ĐÚNG + tường minh
```
d = _parse_date(_cell(row, 0))
if d is None: continue      # ヘッダ行・日付でない行はスキップ  ← tường minh
if not (start <= d < end): continue
```
Dùng JST, KHÔNG dính bug `if dt is not None and dt < cutoff` của chatwork.
→ **S2 cũng propensity, xử lý dòng-biên đúng khi yêu cầu nêu rõ.**

**Verdict: PASS (tĩnh)** — lọc ngày + dòng biên đúng; "今週=đến CN hay thứ Sáu" nên là open point; model rỗng.
