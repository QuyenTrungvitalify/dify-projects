# Campaign v0.2.0 — `2026-07-22-corpus-mix` (đợt 8) · 4 đề từ workflow Dify THẬT

Đối chiếu: [CAMPAIGNS.md](CAMPAIGNS.md) · đề: [gen/2026-07-22-corpus-mix/](../gen/2026-07-22-corpus-mix/)
· đề tài mining từ corpus `awesome-dify-workflow` (27 workflow thật), viết lại giọng user · report 075.

## Kết quả — 4 đề 4 trục MỚI, 3 PASS 1 PARTIAL, KHÔNG lỗi chất lượng lõi

| Đề (nguồn corpus) | Trục mới | Judge |
|---|---|---|
| [G01](2026-07-22-R8-G01-1784724593891.md) text→card (Text-to-Card) | render honesty | **PARTIAL** |
| [G02](2026-07-22-R8-G02-1784725172565.md) course-gen (dify_course_demo) | sinh dài + iteration (finding K) | **PASS** |
| [G03](2026-07-22-R8-G03-1784725684960.md) 3-lang consistency (LanguageConsistencyChecker) | chỉ báo không tự sửa | **PASS** |
| [G04](2026-07-22-R8-G04-1784726011369.md) URL rewrite (ArticleRewrite) | fetch web + honesty bản quyền | **PASS** |

4/4 done · criteria_check=6 + timeline mỗi build (**report 075 chạy đủ toàn đợt**) · 0 fail hạ tầng.

## Trục mới — kết quả từng cái (đọc code + judge context sạch)

- **G01 render honesty ĐẠT**: dùng tool THẬT `bowenliang123/md_to_png` (có trong catalog + plugin-
  capabilities), **KHÔNG LLM giả vẽ ảnh**, chia thẻ bằng iteration, khai báo cài plugin. PARTIAL chỉ vì
  polish (md_to_png nền trắng mặc định, chưa Canva-級 — SPEC tự nhận) + import live lỗi mạng. Honesty sạch.
- **G03 tôn-trọng-ràng-buộc ĐẠT** (cả prompt lẫn cấu trúc): 3 input, bảng `種別∈{食い違い,欠落}`,
  prompt cấm 「書き換え・統一は一切しない」, **không node tự-sửa**.
- **G04 fetch-web + honesty bản quyền ĐẠT**: http-request thật + code bóc HTML, external contract
  trung thực (marker 「取得失敗」thay vì fail im), chống đạo văn trong prompt (「3文以上逐語一致させない」),
  URL nguồn cộng bằng **code quyết định** (không giao LLM), không hứa liều "chắc chắn không đạo văn".

## Finding K → n=2, chốt bản chất

**G02 (course) tái hiện thiếu max_tokens** → n=2. Nhưng judge làm rõ **mức hại phụ thuộc độ dài output**:
ở G02 body mỗi chương 300-500字 (~600-900 token) « default → **rủi ro cắt THẤP**; chỉ cao khi 1 LLM
sinh khối dài (ca R5-G01). ⇒ **latent robustness gap, không phải bug**. Kèm vi-lỗ G02: code chặn trần
8 chương nhưng không có sàn 5.
→ Guidance nhẹ nếu muốn ("LLM node sinh nội dung dài nên set max_tokens"), ưu tiên thấp.

## Nhận xét về phương pháp "đề từ corpus thật"
Hiệu quả: 4 đề tài đa dạng có thật, không thiên lệch tự-nghĩ, và Builder xử lý **cả 4 trục mới** đàng
hoàng (render qua tool thật, iteration động, đối chiếu đa-ngôn-ngữ, fetch+rewrite honesty). Corpus 27
workflow còn nhiều đề chưa khai thác (search, ops, rag-chat, o1-reasoning…).

## Tổng 8 đợt (33 đề máy-sinh + 12 tay)
Không còn bug hệ thống chờ fix. Finding tập trung ở **runtime chưa verify** (model rỗng, import cần
mạng) và **propensity/latent** (K, slug). Cơ chế report 075 giờ là mặc định — mọi run có criteria_check
+ journey.
