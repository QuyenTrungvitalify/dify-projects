# P06 — Internal-rules chatbot (RAG chatflow) · run 1784361880397 · 2026-07-18

**Verdict: PASS mạnh** — lớp mode `advanced-chat` (khác hẳn workflow) xử lý đúng: chatflow validator,
dataset-id không bịa, nhánh từ chối khi ngoài tài liệu, note hướng dẫn setup Knowledge bằng tiếng thường.

## Prompt (nguyên văn)
```
就業規則、経費精算ルール、出張旅費規程などのPDFが10個くらいあります。社員が「出張のときの
日当はいくら？」「有休の繰越は何日まで？」みたいに聞いたら、該当するルールを引用付きで
答えてくれるチャットボットを作りたいです。ルールに書いていないことを聞かれたら、勝手に
推測しないで「規程に記載がないので総務部に確認してください」と答えてほしいです。
```

## Chạy
| | |
|---|---|
| taskId | 1784361880397 · fired 2026-07-18 17:04:40 +09 |
| kết cục | done · ④ · error=none |
| model (mọi phase) | **claude-haiku-4-5** |
| turn | ① 9 · ② 10 · ③ 16 |
| ③ oracle | **call 15 · grep-denied 0** |
| build | **mode=advanced-chat** · 6 node: start · knowledge-retrieval · if-else · llm · answer ×2 |

## Chấm theo checklist P06

| Điểm chấm | Kết quả |
|---|---|
| **mode advanced-chat** + answer node (validator chatflow)? | ✅ mode đúng, 2 answer node, 4 linter sạch. (Bài học campaign #6 — chatflow validator — giữ vững.) |
| **dataset id KHÔNG bịa** (workspace-local)? | ✅ `dataset_ids: []` rỗng — không bịa id. Config đầy đủ: `query_variable_selector:[sys,query]`, `top_k:4`, `score_threshold:0.5`. |
| Note hướng dẫn user tạo Knowledge (việc ngoài workflow)? | ✅ **plain-language**: "a knowledge base to search — pick one in Dify (this step has none selected yet)" — không jargon "knowledge-retrieval/dataset_id". |
| **Nhánh từ chối** 「規程に記載がない」 khi ngoài tài liệu? | ✅ if-else 1 nhánh + câu 「規程に記載がない…総務部に確認」 xuất hiện 2 lần → có nhánh xử lý "không tìm thấy". |
| Trích dẫn (引用付き)? | shape có (retrieval → LLM); chất lượng trích là MANUAL. |
| comprehension (0 jargon)? | ✅ PASS. |

## Ghi chú bộ đo
Toàn Haiku, ③ 0 grep-denied — chatflow không dính lỗ 071 (không cần trigger example). Xác nhận lớp
mode advanced-chat build sạch như workflow.

## MANUAL còn nợ
- Tạo Knowledge trong Dify + upload 10 PDF thật (việc NGOÀI workflow — note đã nói đúng).
- Chất lượng retrieval trên văn bản quy định JA (chunking) — hỏi 5 câu có + 3 câu không có, đếm
  tỉ lệ trích đúng / từ chối đúng.
- Multi-turn: hỏi nối tiếp có giữ ngữ cảnh không.
