# G01 — PDF đào tạo → 20 câu 4択 → tích vào Google Sheets

**Prompt**: [gen/2026-07-20-quiz-gen/G01](../gen/2026-07-20-quiz-gen/G01-training-quiz-sheets.md) · **Run**: `1784522395970`
**Mode**: `auto` · deploy none · ⚠️ **model không pin**: ①② `opus-4-8[1m]`, ③ `haiku-4-5`

| Phase | Model | Turn | Denied |
|---|---|---|---|
| ① | opus-4-8[1m] | 5 | 1 |
| ② | opus-4-8[1m] | 17 | 8 |
| ③ | haiku-4-5 | 30 | 11 |

**Lint 4/4 sạch** · comprehension PASS · digest JA đúng · out: `projects/_drafts/pdf_4_20_1/workflows/main.yml`

## Trục chính: đường tool-catalog-TRÚNG — LẦN ĐẦU có mẫu, ĐẠT

`dependencies:` mang `omluc/google_sheets:0.0.2@17f06eaa…` — **khớp `templates/tool-catalog.json`
từng ký tự** (resolve offline, không bịa, không hạ cấp http-request). P07 chứng minh đường
catalog-trượt; run này đóng nốt nhánh catalog-trúng.

## Judge (context sạch, không thấy ghi chú bẫy): PASS có bảo lưu

7/7 yêu cầu có mặt kèm bằng chứng; hai chỗ khó xử lý đúng cách: cân bằng đáp án **bằng code**, append
Sheets 2 bước đọc-tính-ghi (không đè dòng cũ), SPREADSHEET_ID là env secret. Bảo lưu:

1. **Cân bằng `i % 4` tất định → pattern đáp án 1-2-3-4 lặp cố định, học viên đoán được** — "đúng
   chữ, phản tinh thần yêu cầu". Kèm rủi ro: giải thích LLM nhắc số thứ tự lựa chọn thì sau swap bị
   lệch tham chiếu (prompt không cấm).
2. Số cứng (20 câu, 4 lựa chọn, 5/mức vị trí) chỉ ràng ở prompt — LLM trả 18 câu thì lặng lẽ ghi 18.
3. **Notes ④ toàn tiếng Anh** với persona JA; câu cụt "you need to: an AI model"; boilerplate
   "can't summarize" sai ngữ cảnh; không nhắc OAuth Google + `SHEET_TAB` mặc định `Sheet1`.

## Đối chiếu bẫy đã gài

Cấm-bịa: vào system prompt + `context.enabled: false` + chỉ nối text trích ✅ · cân bằng bằng code ✅
(nhưng sinh bẫy MỚI ở trên) · 4 cột sheet khớp, "正解" ghi số vị trí — SPEC khai open question đúng
mực ✅ · model rỗng → "Model not exist" lúc chạy (nợ đã biết, notes có nhắc — bằng EN) ⚠️

## MANUAL còn nợ

Chạy với PDF thật (bám tài liệu? đủ 20? 誤答 có まぎらわしい?) · render array `text` của extractor
vào prompt · format response thật của plugin batch_get/update · OAuth Google + tab thật.

**Verdict: PASS** (bảo lưu 3 điểm trên) · slug `pdf_4_20_1` = mẫu mới cho finding slug-rác.
