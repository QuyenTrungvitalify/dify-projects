# R3-G03 — Sáng thứ Hai lọc deadline từ Sheets, gom theo người (JA)

**Prompt**: [gen/2026-07-20-ops-mix/G03](../gen/2026-07-20-ops-mix/G03-weekly-deadline-digest.md) · **Run**: `1784547574709`
**Model**: haiku cả 3 · ①14t ②9t ③29t · **failed_split ③: denied≈7 / errored≈5** (mổ dưới)
**Lint 4/4 sạch** · out `projects/_drafts/google_9_chatwork_b/workflows/main.yml`

## Judge (context sạch): PASS (tĩnh) — build tốt nhất đợt

6/6 yêu cầu có bằng chứng dòng; format khớp mẫu 「田中さん:A案件(水)、B案件(金)」 gần từng ký tự
(tự thêm さん chống double, 曜日 1 chữ). **Cả HAI bẫy timezone đều bị né chủ động**:
- trigger `cron 0 9 * * 1` + `timezone: Asia/Tokyo` tường minh (kèm comment cảnh báo UTC default);
- code lấy ngày chạy bằng `utcnow()+9h` có chú thích sandbox không zoneinfo — **không naive now()**.
→ Bài học §9 (2026-07-13) **không tái phạm** ở mẫu thứ hai — propensity giữ.

Thứ tự lọc chuẩn (loại 完了 trước, nên 期限切れ-mà-完了 bị loại đúng nghĩa nguyên văn); 「今週中」
= 月〜日 là **open question trong SPEC** chứ không tự quyết ngầm; parse_date chịu 4+ format;
parse_rows phòng thủ nhiều shape batch_get.

**Trừ (không đổi verdict)**: open question 月〜日 chỉ ở SPEC, notes user-facing không nói · sheet
trống và fetch-lỗi trông giống nhau trên room ("không có案件") · ngày sai format bị nuốt im ·
notes không nói sheet phải tên `シート1` đúng thứ tự cột A–D.

## Mổ ③ denied≈7 (mốc phải mổ) — finding 7 tái diễn LẦN 3 + 1 vi-lỗ mới

- ~5 lệnh săn `frequency: weekly` / per-tool `batch_get` trong catalog: **không có đường tra
  per-tool behavior và không tra được "schedule weekly viết thế nào"** (vi-lỗ mới: pattern
  scheduled-fetch chỉ có mẫu daily);
- `Grep` tool lỗi ×2 (7a còn nguyên) · `python -c` bị chặn đúng thiết kế ×1;
- errored≈5 = linter self-correct bình thường — **nhờ failed_split S5, không còn đọc nhầm thành
  thrash 12**.

## MANUAL còn nợ

Shape THẬT của batch_get (nếu trả markdown thay JSON → mãi báo "không có案件" không lỗi) · cron+
timezone trên Dify 1.15 thật · Chatwork token/room · ngày `7/24` năm ngoái (bị hiểu thành năm nay).

**Verdict: PASS (tĩnh)** · slug `google_9_chatwork_b` = mẫu slug-rác thứ 6.
