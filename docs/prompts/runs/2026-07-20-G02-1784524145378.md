# G02 — Đề trắc nghiệm VI 3 mức, đề/đáp án tách riêng

**Prompt**: [gen/2026-07-20-quiz-gen/G02](../gen/2026-07-20-quiz-gen/G02-vi-quiz-answer-split.md) · **Run**: `1784524145378`
**Mode**: `auto` · deploy none · model: ①③ haiku, ② opus-4-8[1m]

| Phase | Model | Turn | Denied |
|---|---|---|---|
| ① | haiku-4-5 | 5 | 1 |
| ② | opus-4-8[1m] | 9 | 2 |
| ③ | haiku-4-5 | 23 | 5 |

**Lint 4/4 sạch** · comprehension PASS · out: `projects/_drafts/t_i_ph_tr/workflows/main.yml`

## Trục 1: lang-sync VI không-base — digest/SPEC ĐÚNG tiếng Việt

`overview` + SPEC.md thuần Việt → **mẫu ĐÚNG thứ 2** cho nghi vấn "digest lệch ngôn ngữ khi
không-base" (P12-EN vẫn là mẫu sai duy nhất, chưa tái hiện). NHƯNG **notes ④ toàn tiếng Anh** —
xem finding L bên dưới.

## Trục 2: tách kênh đề/đáp án — ĐẠT BẰNG KIẾN TRÚC

Judge (context sạch) soi từng nhánh code kể cả ca JSON lệch shape: LLM xuất JSON có cấu trúc →
code dựng 2 chuỗi riêng `de_phat`/`dap_an` → end trả 2 field tách; `de_lines` **không có dòng nào
ghi đáp án**; parse hỏng → cả hai field cùng nhận thông báo lỗi (không rò); field lạ bị bỏ qua.
Đường rò duy nhất còn lại: LLM nhét "(đáp án: B)" vào ngay chuỗi câu hỏi — không đỡ được bằng cấu
trúc, prompt đã cấm, cần chạy thật.

## Judge verdict: PARTIAL

- **Notes ④ tiếng Anh chứa chỉ dẫn sống còn** ("you need to: an AI model — add one in Dify first")
  — user VI coi như không được báo → workflow không chạy được sau import mà không biết vì sao.
- **SPEC hứa đề xuất `langgenius/anthropic` + resolve hash, YAML ship `dependencies: []`** — lệch
  SPEC↔Implement (model rỗng là nợ đã biết; điểm mới là *lời hứa SPEC không được giữ*).
- Số cứng (15 câu, 4 lựa chọn, 5/mức) ràng ở prompt; code tolerate lệch (nhận câu ≥2 lựa chọn, tổng
  ≠15 chỉ ghi chú tiếng Việt cuối đề — chọn "ít mà thật" hơn "đủ mà bịa", khai ở SPEC Open questions).

## MANUAL còn nợ

Chạy với tài liệu VI thật · đọc bản đề tìm chỗ suy ra đáp án · đếm 5-5-5 · render array text vào prompt.

**Verdict: PARTIAL** (vì notes-EN + model rỗng; phần lõi tách kênh đạt) · slug `t_i_ph_tr` ("Tôi
phụ trách…" bị lột dấu) = **mẫu VI ĐẦU TIÊN** cho finding slug-rác — không còn là chuyện riêng JA.
