# P07 — 議事録 → 決定事項/宿題 抽出 → Google ドキュメント追記

**Prompt**: [P07-minutes-to-docs.md](../P07-minutes-to-docs.md) · **Run**: `1784388534562` (lần 2)
**Lần 1** `1784379467247`: `error` — **đứt mạng API**, không phải lỗi build. Builder park `error`, không
giả done. Chạy lại nguyên văn.
**Mode**: `auto` · `deploy: none` · **model cả 3 phase**: `claude-haiku-4-5`

| Phase | Turn | Thời gian | Call `✗` |
|---|---|---|---|
| ① analyze | 14 | 238s | 7 |
| ② spec | 13 | 246s | 6 |
| ③ implement | 25 | 415s | 5 |

**Lint**: 4/4 sạch (`validate` `lint_refs` `lint_plugin_hashes` `lint_node_bodies` = 0)
**Output**: `projects/_drafts/1_google_1/workflows/main.yml`
**Shape**: `start → llm → code → http-request → end` · `dependencies: []`

## Đạt: không bịa plugin

Trục test của P07 là **plugin không có trong catalog** (Google Docs). Build **không bịa** một
`@sha256` nào — `dependencies: []` — mà đi đường trung thực: `http-request` tới Google Docs API với
`api-key`/`bearer`, và ④ nói thẳng client phải nạp `GOOGLE_DOC_ID` + `GOOGLE_ACCESS_TOKEN`.

Tên app trong YAML đúng tiếng Nhật: 「議事録の決定事項・宿題 抽出 → Googleドキュメント追記」.

## 18 call `✗` — soi từng cái, ra 3 vấn đề

### 1. KHÔNG có đường tìm-chữ nào chạy được (nặng nhất)

```
① Grep  [Gg]oogle.?[Dd]oc                       ✗   ← TOOL lỗi
① Grep  docs                                     ✗   ← TOOL lỗi
① Bash  grep -rin "google" docs/plugin-capabilities.md   ✗   ← GATE chặn
③ Bash  grep -in "google" templates/tool-catalog.json    ✗   ×3
```

Model hỏi một câu hoàn toàn chính đáng — *"repo này có Google Docs không?"* — rồi thử **cả hai**
đường: `Grep` (tool **lỗi**) và `grep` (gate **chặn**). Không đường nào chạy. Đây đúng là lỗ hổng
「permission ≠ availability」 đã gặp: `Grep` nằm trong `permissions.allow` nhưng thực tế lỗi, nên hint
của gate trỏ sang `Read`/`find.py --has` là đúng — mà `find.py --has` chỉ tra **feature của template**,
không đọc được `tool-catalog.json` hay `plugin-capabilities.md`.

Còn lại đúng một đường: `Read` cả file. Model có làm — nhưng chỉ sau 6 lần thử trượt.

### 2. `--dump-schema` trả lời ĐÚNG nhưng báo là lỗi → **đã fix**

```
③ Bash  .venv/bin/python tools/dify_base/lint_node_bodies.py --dump-schema http-request  ✗
```

Không phải bị chặn. Script chạy, in ra đúng câu cần nghe — *"`http-request` là `_error` dump-stub —
lấy shape từ nguồn đã kiểm"* — rồi **`exit 2`**. Với model, `✗` đọc thành *"bị từ chối, tìm đường
khác"*: đúng cái vòng lặp mà `--dump-schema` sinh ra để chấm dứt. Và nó thổi phồng chính
`denied_calls_max` — oracle của spec 071 S2.

**Fix** (đợt này): type **sai chính tả** vẫn `exit 2` (lỗi của người gọi); type **có thật mà schema
không có chi tiết** → `exit 0`, in ra stdout. Không biết là giới hạn của schema, không phải lỗi người
gọi. Test `test_dump_schema_error_stub_type_exits_zero` chốt lại.

### 3. `| head` / `2>/dev/null` bị chặn — gate đúng, chỉ tốn lượt

```
② Bash  cat templates/tool-catalog.json 2>/dev/null | head -200          ✗
② Bash  .venv/bin/python tools/dify_base/find.py --list-features 2>/dev/null | head -80  ✗
```

Cả hai là lệnh `spec.md` **bảo model chạy** — chỉ thêm pipe/redirect nên rớt cửa metacharacter.
Hint của gate đã nói đúng việc cần làm ("chạy lại thành MỘT lệnh trơn, output trả về đủ"), và model
sửa được ngay. Không phải bug — nhưng mỗi lần vẫn tốn một lượt.

### Ngoài ra: `marketplace.py search` bị chặn ×4 → Finding 5 (chưa fix, cố ý)

Đúng như đã biết: catalog offline thiếu Google Docs, và `marketplace.py` bị chặn vì **có network**
(mở kênh exfil qua query param). Build vẫn ra kết quả trung thực bằng `http-request`.

## Vấn đề mới: slug từ prompt tiếng Nhật ra rác

Project ra tên **`1_google_1`** / "1 Google 1". `deriveSlugName` strip hết ký tự ngoài `[a-z0-9]`;
guard `GENERIC_SLUG` chỉ bắn khi requirement **sạch bóng ASCII**. Prompt JA này lại có mảnh ASCII
lạc: 「1万字」→`1`, 「Googleドキュメント」→`google`, 「1万字くらい」→`1`. Ba mảnh vô nghĩa đó
qua được guard và thành tên thư mục.

Không sai chức năng (YAML mang tên JA đúng, human sửa được ở gate ②) nhưng tên thư mục vô nghĩa.
**Chưa fix** — sửa `deriveSlugName` đụng đường đặt tên thư mục đang có + `slug.test.ts`, nên tách
quyết định riêng.

## Kết luận

**PASS** — trục "plugin ngoài catalog" giữ vững: không bịa hash, fallback trung thực, ④ nêu đủ thứ
client phải nạp. Chi phí: ③ 415s với 18 call trượt, mà **quá nửa là do repo không có đường tra cứu**
chứ không phải model lạc.
