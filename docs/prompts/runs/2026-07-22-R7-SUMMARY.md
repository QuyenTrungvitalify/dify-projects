# Campaign v0.2.0 — `2026-07-22-data-integrity` (đợt 7) · verify S1/S2 bằng report MỚI (075)

Đối chiếu: [CAMPAIGNS.md](CAMPAIGNS.md) · đề: [gen/2026-07-22-data-integrity/](../gen/2026-07-22-data-integrity/)
· findings: [spec 076](../../specs/076-build-quality-findings.md) · **lần đầu dùng cơ chế report 075**
(criteria_check + journey) trên backend mới.

Mục đích kép: (1) nghiệm thu report mới trên đợt thật; (2) verify tĩnh 2 finding chưa chắc. 2/2 done.

## Report 075 hoạt động thật
Cả 2 build có `report.json.criteria_check` (**6 tiêu chí/build, đều manual** đúng — câu hành vi) +
`timeline`. journey render đủ 4 phase với thời gian thật (G01 tổng 489s, G02 525s). Đây là lần đầu
report **mở đầu bằng hành trình + tiêu chí build tự cam kết**, không phải judge đọc tay.

## VERIFY tĩnh — cả 2 finding LẬT: propensity, KHÔNG phải bug hệ thống

### S1 (ghi dồn Sheets) — build làm ĐÚNG khi yêu cầu nêu rõ
G01 ([1784713103022](2026-07-22-R7-G01-1784713103022.md), `1_google_1_2`): yêu cầu nhấn "前の日のが
消えないように" → build **đọc-rồi-ghi chuẩn**:
```
existing = <đọc từ batch_get>
rng = sheet + "!A" + str(existing + 1)     # ghi vào dòng KẾ, không đè
payload = [{"range": rng, "values": [row]}]  # sheet rỗng → header + row ở A1
```
Có `read_tail` node + 2 batch_get. Build còn **tự đặt tiêu chí** "追記 dưới dòng cuối, không ghi đè".
→ Trái ngược `google_1_80_chatwork` (đè A:D) — khác nhau ở **độ nhấn của yêu cầu**, không phải năng lực.

### S2 (lọc cửa sổ thời gian) — build xử lý dòng biên ĐÚNG + tường minh
G02 ([1784713596997](2026-07-22-R7-G02-1784713596997.md), `workflow_4`):
```
d = _parse_date(_cell(row, 0))
if d is None: continue      # ヘッダ行・日付でない行はスキップ ← tường minh, có comment
if not (start <= d < end): continue   # ngoài tuần → bỏ
```
Dùng JST. **KHÔNG** dính bug `if dt is not None and dt < cutoff` của `chatwork`. → cũng propensity.

## Kết luận cho spec 076
**Không finding nào là bug hệ thống cần fix.** Cả S1 và S2: khi requirement **nêu rõ mối lo**, Builder
sinh code đúng; các ca "sai" trước đó là khi mối lo **phụ, lẫn trong yêu cầu đa-tính-năng**. Cùng họ
với H (propensity theo độ mạnh tín hiệu).

**Còn nợ runtime** (nửa verify không làm được tĩnh): plugin `omluc/google_sheets` batch_update với
`[{range:"A{n+1}"...}]` có thật sự ghi đúng dòng đó trên Dify 1.15 không — cần 1 lần chạy live. Nhưng
rủi ro "mất dữ liệu hệ thống" mà spec cũ lo → **đã hạ**: build BIẾT cách append đúng.

## Hướng đề xuất (nhẹ hơn hẳn bản 074 cũ)
Không fix code. Cân nhắc **guidance** implement.md: "khi requirement có ghi-dồn/lọc-thời-gian, luôn
đọc-rồi-append + xử lý dòng-thiếu-ngày tường minh **kể cả khi là yêu cầu phụ**" — để nâng propensity
lên cả các ca mối-lo-phụ. Bán kính ≈ 0. Chỉ làm nếu tái hiện ca sai ở yêu cầu đa-tính-năng.
