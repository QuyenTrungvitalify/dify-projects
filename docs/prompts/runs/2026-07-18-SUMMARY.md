# Tổng kết đợt chạy 2026-07-18 — 4 prompt (P09, P11, P04, P01)

**Mục tiêu**: chạy các prompt "user thật" trong `docs/prompts/` qua harness `/e2e` để đánh giá độ
ổn định luồng 4 phase. Server: `dist` 2026-07-17 17:13 (fix tới `c68ef95`, không lẫn WIP đang sửa).

## Bảng kết quả

| Prompt | Trục chính | Kết cục | Tổng | ③ time | Verdict |
|---|---|---|---|---|---|
| P09 vague-sales | sự mơ hồ | done | 322.8s | 109.8s | ✅ PASS mạnh |
| P11 phone-call | từ chối trung thực | done¹ | 850.9s | 490.8s | ⚠️ PASS chất lượng, ③ chậm |
| P04 form-routing | webhook 4-nhánh | done | 839.0s | 522.1s | ⚠️ PASS chất lượng, ③ chậm |
| P01 news-pipeline | honesty phạm vi | done | 655.8s | 320.8s | ✅ PASS mạnh |

¹ P11 lần 1 fail vì hết quota subscription (không phải bug — xem báo cáo P11).

## Kết luận về độ ổn định luồng 4 phase

**Chất lượng build: 4/4 đạt.** Mọi build qua 4 linter, comprehension 0 jargon, digest đúng ngôn
ngữ. Các hành vi "đúng đắn" then chốt đều giữ:
- **Xử lý mơ hồ** (P09): biến 2 câu mù mờ → digest rõ + câu hỏi mở + mặc định an toàn, 0 side-effect thừa.
- **Trung thực dưới áp lực** (P11): không hứa liều 「できますよね？」, không bịa plugin STT (http + placeholder).
- **Honesty phạm vi** (P01): user hỏi "全部できる？無理な部分は？" → chọn lát khả thi + nêu 5 điểm khó,
  **điền plugin hash thật** thay vì `dependencies: []` (tránh lỗi 067).
- **Ranh giới an toàn** (P04): route đúng, その他 default, truyền nguyên văn field.

**Vấn đề hiệu năng: 1 lớp, đã có spec.** Hai build `trigger-webhook` (P11, P04) tốn ③ ~500s vì repo
chưa có pattern webhook; build schedule phức-tạp-hơn (P01) chỉ 320s vì CÓ pattern. Đây là **đối chứng
A/B** củng cố **spec 071** — không phải suy đoán:

| entry | pattern? | ③ call | grep denied | ③ time |
|---|---|---|---|---|
| schedule (P01, 12 node + tool) | ✅ | 21 | 2 | 320s |
| webhook (P04, 10 node) | ❌ | 60 | 7 | 522s |
| webhook (P11, 11 node) | ❌ | 96 | 17 | 490s |

## Việc phát sinh (cho backlog / spec)

1. **Spec 071 S1+S3+S4 giờ có 3 nguồn promote + bằng chứng cứng** — promote `webhook-per-row-notify`
   từ P04/P11/build-071 sẽ cắt ~200s mỗi build webhook. Đây là fix hiệu năng đáng giá nhất tìm được.
2. **Nhu cầu MỚI chưa có spec — tra plugin theo năng lực**: P11 grep 10 lần tìm STT plugin
   (whisper/speech2text/…) vì không có `find.py --plugin-capability`. Lặp lại ở P01 (tìm Sheets/Search
   tool). Đề xuất: một lệnh tra "workspace/marketplace có tool làm việc X không".
3. **Vận hành**: chạy loạt prompt phải canh quota subscription — mỗi build 2–4 turn; P11 lần 1 cạn
   giữa chừng. Builder park `error` đúng cách, không giả done.

## Còn lại trong kho (chưa chạy đợt này)
P02 (OCR ảnh) · P03 (schedule weekday) · P05 (2-file reconcile) · P06 (RAG chatflow) · P07 (tool
Docs) · P08 (glossary) · P10 (VI lang-sync) · P12 (edit-existing, cần base). Ưu tiên tiếp: P03
(weekday bẫy visual_config), P06 (chatflow — lớp mode khác), P10 (VI lang-sync).
