# Campaign v0.1.0 — 12 prompt user-thật qua /e2e · 2026-07-18

Đối chiếu qua các đợt: [CAMPAIGNS.md](CAMPAIGNS.md) · fix đã ship sau đợt này:
[CHANGELOG v0.2.0](../../../apps/builder/CHANGELOG.md) · bộ đo:
[METERING-RELIABILITY.md](METERING-RELIABILITY.md)

**Mục tiêu**: đánh giá độ ổn định luồng 4 phase bằng prompt "user thật" (JA + VI), 12 trục khác nhau.
**Version test**: Builder v0.1.0 (`dist` 2026-07-17, fix tới `c68ef95`).
**Mode**: `auto` · `deploy: none` · build đáp vào `projects/_drafts/` (gitignored).

## Bảng kết quả (12/12 đã chạy)

| Prompt | Trục chính | ③ denied | Verdict |
|---|---|---|---|
| [P01](2026-07-18-P01-1784359444404.md) news-pipeline | honesty phạm vi | 2 | ✅ PASS mạnh |
| [P02](2026-07-18-P02-1784375098683.md) screenshot-OCR | vision/ảnh | 0 | ✅ PASS mạnh |
| [P03](2026-07-18-P03-1784361257820.md) morning-news | schedule 3-bẫy | 0 | ✅ PASS mạnh |
| [P04](2026-07-18-P04-1784358486934.md) form-routing | webhook 4-nhánh | **7** | ⚠️ PASS, ③ chậm |
| [P05](2026-07-18-P05-1784375623443.md) invoice-reconcile | 2-file, cấm-LLM-đụng-số | 0 (retry) | ⚠️ lần 1 lỗi ②, **retry PASS mạnh** |
| [P06](2026-07-18-P06-1784361880397.md) rag-chatbot | advanced-chat mode | 0 | ✅ PASS mạnh |
| [P07](2026-07-18-P07-1784388534562.md) minutes-to-docs | plugin ngoài catalog | **5** (18 cả 3 phase) | ⚠️ lần 1 đứt mạng, **retry PASS** |
| [P08](2026-07-18-P08-1784380142492.md) glossary-translate | multi-step tự soát | 0 | ✅ PASS mạnh |
| [P09](2026-07-18-P09-1784350435308.md) vague-sales | sự mơ hồ | 0 | ✅ PASS mạnh |
| [P10](2026-07-18-P10-1784362368139.md) contract-VI | lang-sync VI | 2 | ✅ PASS mạnh |
| [P11](2026-07-18-P11-1784357457370.md) phone-call | từ chối trung thực | **17** | ⚠️ PASS, ③ chậm |
| [P12](2026-07-18-P12-1784380636506.md) edit-existing | sửa flow có sẵn | 3 | ⚠️ 2 vấn đề |

## Kết luận: 11 build đạt chất lượng · KHÔNG có lỗi chất lượng nào

Mọi build qua **4 linter sạch**, **comprehension 0 jargon**, digest đúng ngôn ngữ (trừ P12 lần
không-base). Các hành vi đúng đắn then chốt giữ vững qua mọi trục:

- **Không bịa**: plugin STT (P11), dataset id (P06), plugin Docs (P07 retry → fallback http trung thực)
- **Tôn trọng ràng buộc user**: cấm auto-send (P05: 0 node gửi mail) · cấm đổi flow cũ (P12 có base:
  **giữ 100% node id gốc**) · cấm suy đoán URL (P02) · その他 default (P04)
- **Giao việc đúng chỗ**: phép so tiền/tên do **code** làm, LLM chỉ viết văn (P05)
- **Trung thực**: nêu 5 điểm khó khi user hỏi "全部できる？" (P01) · không hứa liều 「できますよね？」 (P11)

2 ca không hoàn thành đều **không phải lỗi chất lượng** — và Builder **contain đúng cả hai** (park
`error`, không giả done): P05 lần 1 (② hỏi thay vì viết — propensity 1/2, retry pass) · P07 lần 1
(đứt mạng API).

## 5 finding → đã fix trong v0.2.0

| # | Finding | Bằng chứng | Fix |
|---|---|---|---|
| 1 | Webhook thrash ③ (~500s): thiếu pattern + không có đường trích schema | P04/P11 (7–17 denied) vs P01/P03/P06/P10 (0–2), cùng model | spec 071 S1–S4 |
| 2 | ④ không nói client phải **nối nguồn** webhook | build `google_slack`: grep webhook/POST = **0** | spec 072 S1–S3 |
| 3 | ② đôi khi hỏi thay vì viết SPEC.md (auto mode) | P05 lần 1 (retry pass → propensity) | `spec.md` auto-confirm |
| 4 | Harness không fire được edit-existing | P12 `task.workflow = null` | `fire --workflow` |
| 5 | `--dump-schema` trả lời **đúng** cho type không có schema chi tiết nhưng `exit 2` → model đọc thành "bị từ chối", và thổi phồng chính oracle 071 S2 | P07 `--dump-schema http-request` | type có thật → `exit 0`/stdout; chỉ type **sai** mới `exit 2` |

**Verify sau fix**: webhook denied **7→0** và ③ đọc thẳng pattern · edit-existing giữ **5/5 node id
gốc** + digest chuyển JA đúng · advisory nguồn webhook xuất hiện với đúng field của từng build.

## 4 việc CHƯA fix (cố ý — cần quyết phạm vi)

- **`tool-catalog.json` chỉ 6 plugin** (thiếu Google Docs) → ② phải thử đường online rồi bị chặn.
  Fix đúng = **mở rộng catalog offline**. ~~Cho `marketplace.py` vào allow-set~~ **đã bác bỏ**: nó gọi
  network → mở kênh exfil qua query param; gate cấm curl/wget chính vì thế.
- **① digest EN cho prompt JA** (P12 lần không-base). n=1: cùng đợt P02 đúng JA, và P12 **khi có base**
  cũng đúng JA → nghi lệch khi thiếu ngữ cảnh. Chưa đủ mẫu để fix.
- **Không có đường tìm-chữ nào chạy được**: `Grep` (tool) **lỗi** và `grep` (bash) **bị gate chặn** →
  câu hỏi chính đáng "repo có Google Docs không?" phải `Read` cả file mới trả lời được (P07: 6 lần thử
  trượt). `find.py --has` chỉ tra feature của template, không đọc `tool-catalog.json`. Cần quyết: sửa
  tool `Grep`, hay mở một đường tra cứu đọc-only có kiểm soát.
- **Slug từ prompt JA ra rác**: P07 → `1_google_1`. Guard `GENERIC_SLUG` chỉ bắn khi requirement sạch
  bóng ASCII; mảnh ASCII lạc (「1万字」「Google」) lọt qua. Cosmetic, human sửa được ở gate ② — nhưng
  sửa `deriveSlugName` đụng đường đặt tên thư mục + `slug.test.ts` nên tách riêng.

## Bài học vận hành cho đợt sau

1. **Turn không phải oracle** cho thrash (nén call) — dùng **denied-call count**.
2. **Model không pin**: thrash mạnh → CLI nhảy `opus-4-8[1m]`; chỉ so **cùng model**.
3. **Quota**: mỗi build 2–4 turn thật; loạt 12 prompt cạn quota giữa chừng (P11 lần 1) — Builder park
   `error` đúng, chỉ cần chờ reset rồi chạy lại.
4. **n=1 không kết luận**: 2 trong 4 chẩn đoán ban đầu của tôi sai và phải sửa sau khi retry/đọc code.
